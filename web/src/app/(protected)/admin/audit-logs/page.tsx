'use client';

/**
 * /admin/audit-logs — canonical E2E test route (master-flow.md §12).
 * The original audit feature lives at /admin/audit.
 * This page re-exports the same content for backward compatibility.
 */
export { default } from '../audit/page';
