// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"kv-shepherd.io/shepherd/ent/vm"
	"kv-shepherd.io/shepherd/ent/vmrevision"
)

// VMRevision is the model entity for the VMRevision schema.
type VMRevision struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Revision holds the value of the "revision" field.
	Revision int `json:"revision,omitempty"`
	// Spec holds the value of the "spec" field.
	Spec map[string]interface{} `json:"spec,omitempty"`
	// ChangeReason holds the value of the "change_reason" field.
	ChangeReason string `json:"change_reason,omitempty"`
	// ChangedBy holds the value of the "changed_by" field.
	ChangedBy string `json:"changed_by,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VMRevisionQuery when eager-loading is set.
	Edges        VMRevisionEdges `json:"edges"`
	vm_revisions *string
	selectValues sql.SelectValues
}

// VMRevisionEdges holds the relations/edges for other nodes in the graph.
type VMRevisionEdges struct {
	// VM holds the value of the vm edge.
	VM *VM `json:"vm,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// VMOrErr returns the VM value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VMRevisionEdges) VMOrErr() (*VM, error) {
	if e.VM != nil {
		return e.VM, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: vm.Label}
	}
	return nil, &NotLoadedError{edge: "vm"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VMRevision) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case vmrevision.FieldSpec:
			values[i] = new([]byte)
		case vmrevision.FieldRevision:
			values[i] = new(sql.NullInt64)
		case vmrevision.FieldID, vmrevision.FieldChangeReason, vmrevision.FieldChangedBy:
			values[i] = new(sql.NullString)
		case vmrevision.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case vmrevision.ForeignKeys[0]: // vm_revisions
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VMRevision fields.
func (_m *VMRevision) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vmrevision.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case vmrevision.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case vmrevision.FieldRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field revision", values[i])
			} else if value.Valid {
				_m.Revision = int(value.Int64)
			}
		case vmrevision.FieldSpec:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field spec", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Spec); err != nil {
					return fmt.Errorf("unmarshal field spec: %w", err)
				}
			}
		case vmrevision.FieldChangeReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field change_reason", values[i])
			} else if value.Valid {
				_m.ChangeReason = value.String
			}
		case vmrevision.FieldChangedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field changed_by", values[i])
			} else if value.Valid {
				_m.ChangedBy = value.String
			}
		case vmrevision.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vm_revisions", values[i])
			} else if value.Valid {
				_m.vm_revisions = new(string)
				*_m.vm_revisions = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VMRevision.
// This includes values selected through modifiers, order, etc.
func (_m *VMRevision) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryVM queries the "vm" edge of the VMRevision entity.
func (_m *VMRevision) QueryVM() *VMQuery {
	return NewVMRevisionClient(_m.config).QueryVM(_m)
}

// Update returns a builder for updating this VMRevision.
// Note that you need to call VMRevision.Unwrap() before calling this method if this VMRevision
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VMRevision) Update() *VMRevisionUpdateOne {
	return NewVMRevisionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VMRevision entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VMRevision) Unwrap() *VMRevision {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VMRevision is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VMRevision) String() string {
	var builder strings.Builder
	builder.WriteString("VMRevision(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revision))
	builder.WriteString(", ")
	builder.WriteString("spec=")
	builder.WriteString(fmt.Sprintf("%v", _m.Spec))
	builder.WriteString(", ")
	builder.WriteString("change_reason=")
	builder.WriteString(_m.ChangeReason)
	builder.WriteString(", ")
	builder.WriteString("changed_by=")
	builder.WriteString(_m.ChangedBy)
	builder.WriteByte(')')
	return builder.String()
}

// VMRevisions is a parsable slice of VMRevision.
type VMRevisions []*VMRevision
