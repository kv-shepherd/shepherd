// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"kv-shepherd.io/shepherd/ent/systemsecret"
)

// SystemSecret is the model entity for the SystemSecret schema.
type SystemSecret struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// KeyName holds the value of the "key_name" field.
	KeyName string `json:"key_name,omitempty"`
	// KeyValue holds the value of the "key_value" field.
	KeyValue string `json:"-"`
	// Source holds the value of the "source" field.
	Source       systemsecret.Source `json:"source,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SystemSecret) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case systemsecret.FieldID, systemsecret.FieldKeyName, systemsecret.FieldKeyValue, systemsecret.FieldSource:
			values[i] = new(sql.NullString)
		case systemsecret.FieldCreatedAt, systemsecret.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SystemSecret fields.
func (_m *SystemSecret) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case systemsecret.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case systemsecret.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case systemsecret.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case systemsecret.FieldKeyName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key_name", values[i])
			} else if value.Valid {
				_m.KeyName = value.String
			}
		case systemsecret.FieldKeyValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key_value", values[i])
			} else if value.Valid {
				_m.KeyValue = value.String
			}
		case systemsecret.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				_m.Source = systemsecret.Source(value.String)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SystemSecret.
// This includes values selected through modifiers, order, etc.
func (_m *SystemSecret) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this SystemSecret.
// Note that you need to call SystemSecret.Unwrap() before calling this method if this SystemSecret
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SystemSecret) Update() *SystemSecretUpdateOne {
	return NewSystemSecretClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SystemSecret entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SystemSecret) Unwrap() *SystemSecret {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SystemSecret is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SystemSecret) String() string {
	var builder strings.Builder
	builder.WriteString("SystemSecret(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("key_name=")
	builder.WriteString(_m.KeyName)
	builder.WriteString(", ")
	builder.WriteString("key_value=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(fmt.Sprintf("%v", _m.Source))
	builder.WriteByte(')')
	return builder.String()
}

// SystemSecrets is a parsable slice of SystemSecret.
type SystemSecrets []*SystemSecret
