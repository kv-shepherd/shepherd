# OpenAPI operations intentionally not consumed by frontend yet.
# Format: METHOD /path
GET /auth/providers/{provider_id}/callback
POST /auth/providers/{provider_id}/callback
# External approval callbacks are server-to-server webhooks, not frontend calls.
POST /webhooks/approval-callback
# Console websocket endpoints are negotiated via POST /vms/{vm_id}/console/request
# and opened with the browser WebSocket API instead of api.get().
GET /vms/{vm_id}/serial
GET /vms/{vm_id}/vnc
