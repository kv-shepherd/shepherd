# Acceptance Checklist

> **Purpose**: This document is the single acceptance standard  
> **Key Decision**: ADR-0012 Hybrid Transaction Strategy (Ent + sqlc) + CI Blocking Checks
>
> **Note**: Detailed per-phase checklists are now in the [checklist/](./checklist/) directory.
> Non-blocking production, environment, and V2/RFC work is tracked in
> [DEFERRED_FOLLOWUPS.md](./DEFERRED_FOLLOWUPS.md).

---

## Usage Instructions

1. Verify each phase (frontend and backend scope) using the detailed phase checklists.
2. All ✅ required before proceeding to the next phase.
3. ❌ items must be fixed and re-verified.
4. API/flow changes are not complete until OpenAPI, frontend docs, master-flow, and phase docs are all synchronized.
5. Gate-first rule: complete [ci/GATE_HARDENING_CHECKLIST.md](./ci/GATE_HARDENING_CHECKLIST.md) before new feature delivery.

---

## Phase Checklists

| Phase | Checklist | Specification | Status |
|-------|-----------|---------------|--------|
| Phase 0 | [checklist/phase-0-checklist.md](./checklist/phase-0-checklist.md) | [phases/00-prerequisites.md](./phases/00-prerequisites.md) | ✅ Complete (2026-02-09) |
| Phase 1 | [checklist/phase-1-checklist.md](./checklist/phase-1-checklist.md) | [phases/01-contracts.md](./phases/01-contracts.md) | 🔄 Partial (~99%) — 33 Ent schemas + Ent schema standards/codegen/dynamic-query safety gates + TS/Go API types + ADR-0028 generated-type/pointer-audit gates + frontend testing toolchain + cluster credential runtime + provider plugin config boundary ✅; remaining work is deferred V2 schemas/deployment items |
| Phase 2 | [checklist/phase-2-checklist.md](./checklist/phase-2-checklist.md) | [phases/02-providers.md](./phases/02-providers.md) | 🔄 Partial (~73%) — Basic CRUD + SSAApplier + VMRenderer + AuthProvider Admin + KubeVirt instance type/preference catalog reads + label-based pending adoption discovery/periodic scan + admin adoption management + bounded provider K8s operation timeouts + provider unit test lane + V1 River queue concurrency + i18n/Atlas baselines ✅, Snapshot/Clone/Migration deferred; V1 status sync uses ADR-0038 polling baseline |
| Phase 3 | [checklist/phase-3-checklist.md](./checklist/phase-3-checklist.md) | [phases/03-service-layer.md](./phases/03-service-layer.md) | 🔄 Partial (~89%) — Core DI/UseCase + ADR-0012 atomic path + sqlc + InstanceSize handler + analyzer-backed DI/sqlc CI gates ✅, River worker concurrency + V1 degradation baseline ✅, DB-backed VM API CRUD handler success paths + VMService create approval/worker e2e ✅, per-cluster semaphore/degradation UX deferred |
| Phase 4 | [checklist/phase-4-checklist.md](./checklist/phase-4-checklist.md) | [phases/04-governance.md](./phases/04-governance.md) | 🔄 Partial (~98%) — Approval/Audit/Atomic enqueue/Delete/Namespace CRUD/Notification system ✅, environment scheduling + visibility filtering ✅, Stage 5.E batch + queue UX ✅, Stage 6 VNC token hardening ✅, Catalog Scope + Cluster Policy + VM Status Sync (ADR-0038) + Template/InstanceSize validators + User My Requests API ✅; full resource reconciler + rich revision diff/compression service + template lifecycle states deferred |
| Phase 5 | [checklist/phase-5-checklist.md](./checklist/phase-5-checklist.md) | [phases/05-auth-api-frontend.md](./phases/05-auth-api-frontend.md) | ✅ Complete for current scope (2026-05-08) — Backend Auth ✅ (JWT hardening + bcrypt cost 12 + log redaction), 140 endpoints (ADR-0028 omitzero) ✅, 32 App Router page files (including compatibility/alias routes), login/protected API and admin CRUD mock E2E verified |

---

## Cross-Phase Verification

### Master-Flow VM Lifecycle Alignment (audited 2026-02-10)

> **Purpose**: This section is the **coding checkpoint** for VM lifecycle implementation.
> Start here instead of re-reading all backend code.
>
> **Source of truth**: [master-flow.md](interaction-flows/master-flow.md) Part 3 (Stages 5.A–5.F) + Stage 6 + Part 4
>
> **Detailed gaps**: See [phase-4-checklist.md §Master-Flow Alignment Status](checklist/phase-4-checklist.md#master-flow-alignment-status-vm-lifecycle-audited-2026-02-10)

| Stage | Description | Alignment | Checklist Section | Design Doc Reference | Code Fix Priority |
|-------|-------------|-----------|-------------------|----------------------|-------------------|
| **Part 4** | State Machines & Data Models | ✅ 90% | [phase-2 §Anti-Corruption](checklist/phase-2-checklist.md#anti-corruption-layer) | [master-flow §Part 4](interaction-flows/master-flow.md#part-4-state-machines-data-models) | ~~P0~~ ✅ Done (2026-02-10) |
| **5.A** | VM Request Submission | ✅ 90% | [phase-3 §UseCase](checklist/phase-3-checklist.md#usecase-layer-standards-clean-architecture) | [master-flow §5.A](interaction-flows/master-flow.md#stage-5-a) | P2-done — request path creates pending Ticket/DomainEvent without a VM row; VM `PENDING` is K8s scheduler wait only |
| **5.B** | Admin Approval | ✅ 95% | [phase-4 §Approval](checklist/phase-4-checklist.md#approval-flow-governance-core) | [master-flow §5.B](interaction-flows/master-flow.md#stage-5-b) | P3-done — prod overcommit warning UX is surfaced in the approval UI |
| **5.C** | VM Creation Execution | ✅ 97% | [phase-4 §River Queue](checklist/phase-4-checklist.md#river-queue-task-system-adr-0006) | [master-flow §5.C](interaction-flows/master-flow.md#state-transitions-stage-5a-5c) | P3-done — provider create ownership guard rejects unowned same-name VMs before SSA apply |
| **5.D** | Delete Operations | ✅ 96% | [phase-4 §Delete](checklist/phase-4-checklist.md#delete-confirmation-mechanism-adr-0015-131) | [master-flow §5.D](interaction-flows/master-flow.md#stage-5d-delete-operations) | P2-done — VM hard-delete plus periodic tombstone retry cleanup |
| **5.E** | Batch Operations | ✅ 97% | [phase-4 §Batch](checklist/phase-4-checklist.md#batch-operations-adr-0015-19) | [master-flow §5.E](interaction-flows/master-flow.md#stage-5e-batch-operations) | P2-done — API + child execution dispatch + two-layer throttling + parent projection + admin override APIs + `/vms/batch/power` + frontend queue UX (`status_url` tracking/429 cooldown/affected-child feedback/aria-live) and JSON result export complete |
| **5.F** | Notification System | ✅ 95% | [phase-4 §Notification](checklist/phase-4-checklist.md#notification-system-adr-0015-20) | [master-flow §5.F](interaction-flows/master-flow.md#stage-5f-notification-system) | P3-done — V1 inbox flow complete (API+triggers+sender+frontend bell+retention cleanup); external channels remain V2+ scope |
| **6** | VNC Console Access | ⚠️ 96% | [phase-4 §VNC](checklist/phase-4-checklist.md#vnc-console-permissions-adr-0015-18-181-addendum) | [master-flow §6](interaction-flows/master-flow.md#stage-6-vnc-console-access) | V1-baseline done — request/status/open API, approval guard, audit, encrypted single-use bootstrap credential, shared replay marker, and frontend approved-target validation are shipped; noVNC proxy internals and VNC active revocation remain V2+ |

#### Coding Priority Queue (work in this order)

1. ~~**P0** — Fix VM status enum (`domain/vm.go`): `ERROR`→`FAILED`, add `STOPPING`, clarify `PENDING` as K8s scheduler wait~~ ✅ **Done** (2026-02-10)
   - `ERROR`→`FAILED`, added `STOPPING`/`MIGRATING`/`PAUSED`/`UNKNOWN`
   - Ent schema, mapper, OpenAPI spec, all handlers aligned
   - `PENDING` retained as K8s-extended state (not removed, used by mapper)
2. ~~**P1** — Ent codegen + Atlas migration for VM status enum changes~~ ✅ **Done** (2026-02-10)
3. ~~**P1** — Frontend VM status labels/colors sync (`web/src/types/api.gen.ts`)~~ ✅ **Done** (2026-02-10)
   - `api.gen.ts` regenerated from OpenAPI spec
   - `VM_STATUS_MAP` in `vms/page.tsx` updated (APPROVED/ERROR/DELETED → STOPPING/FAILED/MIGRATING/PAUSED/UNKNOWN)
   - i18n keys updated (en + zh-CN)
4. ~~**P1** — Enhance `ApprovalValidator`: dedicated CPU + overcommit check, resource capability matching~~ ✅ **Done** (2026-02-10)
5. ~~**P1** — Fix delete governance: cascade checks, approval ticket for VM delete, `DELETING` state usage~~ ✅ **Done** (2026-02-10)
6. ~~**P2** — Ticket lifecycle: worker updates ticket `EXECUTING`→`SUCCESS/FAILED`~~ ✅ **Done** (2026-02-10)
7. ~~**P1** — Align approval commit path to ADR-0012 (`sqlc + InsertTx`)~~ ✅ **Done** (2026-02-10)
8. ~~**P2/P3** — Batch API + child execution dispatch + two-layer throttling + parent projection + admin override APIs + `/vms/batch/power` + frontend queue UX~~ ✅ **Done** (Stage 5.E baseline complete); ~~Stage 6 VNC baseline with shared PG replay marker~~ ✅ **Done**; ~~VNC credential AES-256-GCM hardening~~ ✅ **Done** (`ENCRYPTION_KEY` + AES-256-GCM encrypted JWT envelope); Notification system ✅ (V1 inbox flow complete); VM Status Sync Worker (ADR-0038) ✅; Catalog Scope separation (ADR-0040) ✅
9. ~~**P1** — Cap approval/audit business metric `action` labels to fixed low-cardinality values~~ ✅ **Done** (2026-06-18)
   - Built-in approval audit actions keep their exact values; blank, unknown, future, or external-provider approval actions aggregate as `approval.other`
   - Covered across Prometheus metrics, admin audit-signal API response normalization, OpenAPI generated docs, and recording-rule fixtures
   - Verified by `go test ./internal/observability ./internal/api/handlers`, `make api-check`, and `make project-completion-readiness`
10. ~~**P2** — Harden frontend console launch target resolution~~ ✅ **Done** (2026-06-18)
   - Approved console responses now accept only same-origin `/api/v1/vms/{id}/vnc` or `/api/v1/vms/{id}/serial` paths matching the requested console type
   - Absolute URLs, protocol-relative URLs, query strings, fragments, and VNC/serial mismatches are rejected before embedding/opening
   - Verified by `cd web && npm test -- --run src/features/vm-management/console.test.ts`
11. ~~**P1** — Make latest full live E2E release-evidence validation fail with actionable status~~ ✅ **Done** (2026-06-18)
   - `make ci-live-e2e-latest-evidence` now prints the selected full manifest before validation and reports a clear "no full live E2E release evidence is available" failure when only preflight or no manifests exist
   - This does not complete real-cluster live E2E; it makes the missing full evidence state explicit and points operators back to `make live-e2e-readiness` plus the full SOP
   - Verified by the expected-failure local check for an environment with no full `.run/live-e2e` manifest
12. ~~**P1** — Require full live E2E release evidence to include cleanup-reviewed run artifacts~~ ✅ **Done** (2026-06-18)
   - Strict full-pass validation now requires evidence/result/runner/backend/Playwright JSON artifacts, cleanup namespace recording, namespace VM cleanup enabled, and runner log cleanup review status `passed`
   - The live runner writes an explicit namespace VM cleanup review marker on exit so release evidence proves cleanup rather than only declaring the requirement
   - Verified by `make ci-live-e2e-evidence`, which now self-tests real artifact presence and rejects a runner log without cleanup `status=passed`
13. ~~**P2** — Extend KubeVirt provider operation-timeout coverage for VM reads and dry-run validation~~ ✅ **Done** (2026-06-18)
   - `GetVM`, `ListVMs`, VMI enrichment, node host-IP enrichment, and `ValidateSpec` server-side dry-run now use the configured K8s operation timeout instead of the raw caller context
   - Added provider tests that capture fake K8s client contexts and assert deadlines are applied on VM, VMI, Node, and SSA dry-run calls
   - Verified by `go test -count=1 ./internal/provider`
14. ~~**P2** — Bound cluster health-check Kubernetes probes with operation timeout~~ ✅ **Done** (2026-06-18)
   - Cluster health checks now create a bounded context for KubeVirt CR probing, capability detection, and StorageClass discovery instead of relying on the caller context alone
   - Production wiring reuses `k8s.operation_timeout`; the legacy constructor remains compatible with a safe default timeout
   - Verified by `go test -count=1 ./internal/provider ./internal/app/modules`
15. ~~**P2** — Add shepherd-linter regression guard for provider-layer K8s operation timeouts~~ ✅ **Done** (2026-06-18)
   - Added `shepherd-arch/k8stimeout` analyzer to reject direct provider-layer K8s client calls that pass raw `ctx` instead of bounded `opCtx`
   - The guard covers KubeVirt provider direct client calls and `ClusterHealthChecker.CheckCluster` probe/delegation calls, with analysistest fixtures for violation and clean cases
   - Verified by `cd tools/shepherd-linter && go test ./...` and `make shepherd-lint`
16. ~~**P2** — Close provider helper escape hatch for K8s operation-timeout enforcement~~ ✅ **Done** (2026-06-18)
   - Extended `shepherd-arch/k8stimeout` from receiver-method-only scanning to typed provider-owned K8s client interface calls across `internal/provider`
   - Fixed helper-level KubeVirt CR, Node, StorageClass, and VM read paths so they receive and forward bounded `opCtx` instead of raw `ctx`
   - Verified by `cd tools/shepherd-linter && go test ./...`, `make shepherd-lint`, and `go test -count=1 ./internal/provider ./internal/app/modules`
17. ~~**P2** — Restore full Go lint CI target after provider timeout test additions~~ ✅ **Done** (2026-06-18)
   - Fixed provider timeout test helper signatures to follow the Go context-first convention enforced by `revive`
   - Removed the now-constant helper timeout parameter so `unparam` stays clean
   - Verified by `make ci-go-lint` and `go test -count=1 ./internal/provider`
18. ~~**P2** — Strengthen ADR-0031 worker-pool context propagation evidence~~ ✅ **Done** (2026-06-18)
   - Worker pool tests now prove queued request-scoped tasks are skipped when their context is cancelled before execution
   - Detached task tests now prove service lifecycle cancellation is delivered to background tasks during pool shutdown
   - Verified by `go test -count=1 ./internal/pkg/worker`, `go test -race -count=1 ./internal/pkg/worker`, and `make ci-go-lint`
19. ~~**P2** — Centralize dependency/toolchain version references in current-state design docs~~ ✅ **Done** (2026-06-18)
   - Removed hardcoded dependency and toolchain versions from the high-level implementation summary docs and replaced them with `DEPENDENCIES.md` links
   - Added a `check_design_doc_governance.sh` guard so `README.md`, `CURRENT_STATE.md`, `frontend/FRONTEND.md`, and Phase 5 docs cannot reintroduce those version pins
   - Verified by `bash docs/design/ci/scripts/check_design_doc_governance.sh`
20. ~~**P2** — Cover infrastructure wiring for health-check K8s operation timeout~~ ✅ **Done** (2026-06-18)
   - Added a same-package module test proving `NewInfrastructure` wires `k8s.operation_timeout` into the cluster health checker
   - This closes the strict test-first delta guard gap for the runtime wiring change in `internal/app/modules/infrastructure.go`
   - Verified by `go test -count=1 ./internal/app/modules` and `make ci-governance`
21. ~~**P2** — Refresh embedded KubeVirt schema and client baseline to latest GA patch~~ ✅ **Done** (2026-06-18)
   - Upgraded embedded InstanceSize KubeVirt schema, `kubevirt.io/api`, and `kubevirt.io/client-go` from `v1.8.2` to `v1.8.4`
   - Kept Kubernetes core packages replace-locked to `v0.34.3`, matching the KubeVirt `v1.8.4` module baseline; schema report found no new exposed fields, changed mask fields, or missing locale keys
   - Verified by `make kubevirt-schema-check`, `make kubevirt-schema-report`, schema tool tests, `go test -count=1 ./internal/provider`, `make ci-go-build`, `make ci-go-lint`, and `make ci-governance`
22. ~~**P2** — Clear frontend Babel audit finding in dependency lockfile~~ ✅ **Done** (2026-06-18)
   - Refreshed the transitive Babel toolchain lock from `@babel/core 7.29.0` to `7.29.7`, resolving the low-severity `sourceMappingURL` arbitrary file read advisory without adding a direct dependency
   - `npm audit --audit-level=low --prefix web` now reports zero vulnerabilities
   - Verified by `make ci-frontend-deadcode`, `npm run lint --prefix web`, and `make ci-frontend-unit-local`
23. ~~**P2** — Strengthen KubeVirt schema upgrade tool coverage~~ ✅ **Done** (2026-06-18)
   - Added tests for Swagger `VirtualMachineSpec` extraction with `$ref` expansion, missing-definition diagnostics, schema diff summary output, and manifest read/write round trips
   - Raised `cmd/kubevirt-schema-upgrade` package coverage from 13.3% to 50.2%, improving evidence around the maintenance path used for KubeVirt schema refreshes
   - Verified by `go test -race -count=1 ./cmd/kubevirt-schema-upgrade`, schema tool package tests, and `make ci-go-lint`
24. ~~**P2** — Strengthen KubeVirt schema diff tool coverage~~ ✅ **Done** (2026-06-18)
   - Added tests for directory bundle loading, source-selection validation, missing file diagnostics, sorted diff helpers, scalar type rendering, and printed diff sections
   - Raised `cmd/kubevirt-schema-diff` package coverage from 38.8% to 67.6%, improving evidence for schema upgrade review tooling
   - Verified by `go test -race -count=1 ./cmd/kubevirt-schema-diff`, schema tool package tests, and `make ci-go-lint`
25. ~~**P2** — Strengthen KubeVirt schema freshness-check coverage~~ ✅ **Done** (2026-06-18)
   - Added tests for manifest parsing failures, GitHub release filtering, request headers/token handling, API error body truncation, invalid JSON, and no-GA-release diagnostics
   - Refactored `fetchLatestGA` behind an injectable HTTP client/URL helper while preserving the production default GitHub releases endpoint
   - Raised `cmd/kubevirt-schema-check` package coverage from 27.7% to 59.3%
   - Verified by `go test -race -count=1 ./cmd/kubevirt-schema-check`, schema tool package tests, and `make ci-go-lint`
26. ~~**P2** — Strengthen live E2E seed fixture coverage without cluster dependencies~~ ✅ **Done** (2026-06-18)
   - Added tests for seed kubeconfig base64/path error handling, explicit API server override, kubeconfig server selection, environment boolean parsing, and live template/InstanceSize fixture shape
   - Raised `cmd/e2e-seed` package coverage from 6.8% to 12.1% while keeping tests free of real database or Kubernetes dependencies
   - Verified by `go test -race -count=1 ./cmd/e2e-seed`, e2e/schema tool package tests, and `make ci-go-lint`
27. ~~**P2** — Document and test the default admin seed bootstrap contract~~ ✅ **Done** (2026-06-18)
   - Centralized the default admin seed constants into a package-private spec used by `seedDefaultAdmin`
   - Added tests proving the Stage 1.5 bootstrap account keeps forced password change enabled and binds to the existing global PlatformAdmin role
   - Raised `cmd/seed` package coverage from 1.1% to 2.1% without requiring PostgreSQL or Kubernetes dependencies
   - Verified by `go test -cover -count=1 ./cmd/seed` and `go test -race -count=1 ./cmd/seed`
28. ~~**P2** — Strengthen VM create helper coverage for InstanceSize storage and typed override parsing~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for DataVolume access-mode/volume-mode precedence, InstanceSize storage hint cloning, string-slice lookup filtering, and integer conversion boundary cases
   - Raised `internal/jobs` package coverage from 23.5% to 26.9% without requiring PostgreSQL or Kubernetes dependencies
   - Verified by `go test -cover -count=1 ./internal/jobs` and `go test -race -count=1 ./internal/jobs`
29. ~~**P2** — Strengthen VM power worker retry/cancel classification coverage~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for start/stop idempotent conflicts, K8s and provider not-found detection, restart invalid-state conflicts, and terminal-versus-retryable power errors
   - Raised `internal/jobs` package coverage from 26.9% to 29.5% without requiring PostgreSQL or Kubernetes dependencies
   - Verified by `go test -cover -count=1 ./internal/jobs` and `go test -race -count=1 ./internal/jobs`
30. ~~**P2** — Strengthen bootstrap security secret validation coverage~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for invalid explicit session/encryption secrets, missing database fallback, stable `system_secrets` IDs, and generated random hex secret shape
   - Raised `internal/infrastructure` package coverage from 20.5% to 24.2% without requiring PostgreSQL dependencies
   - Verified by `go test -cover -count=1 ./internal/infrastructure` and `go test -race -count=1 ./internal/infrastructure`
31. ~~**P2** — Strengthen notification parameter construction coverage~~ ✅ **Done** (2026-06-18)
   - Centralized approval-pending notification parameter construction behind a pure helper used by `OnTicketSubmitted`
   - Added tests for approval-pending i18n/resource/message params and notification parameter map cloning behavior
   - Raised `internal/notification` package coverage from 33.7% to 41.6% without requiring PostgreSQL dependencies
   - Verified by `go test -cover -count=1 ./internal/notification` and `go test -race -count=1 ./internal/notification`
32. ~~**P2** — Strengthen approval snapshot helper coverage~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for approval snapshot string trimming, DataVolume access-mode slice cloning/filtering, missing/invalid snapshot values, and JSON marshal error handling
   - Raised `internal/usecase` package coverage from 35.2% to 37.5% without requiring external services
   - Verified by `go test -cover -count=1 ./internal/usecase` and `go test -race -count=1 ./internal/usecase`
33. ~~**P2** — Strengthen batch power retry validation coverage~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for batch power submission validation, retry validation, SQLC int32 child-count bounds, and uninitialized retry fast-fail behavior
   - Raised `internal/usecase` package coverage from 37.5% to 40.0% without requiring external services
   - Verified by `go test -cover -count=1 ./internal/usecase` and `go test -race -count=1 ./internal/usecase`
34. ~~**P2** — Strengthen VM delete job configuration and state guard coverage~~ ✅ **Done** (2026-06-18)
   - Added pure unit tests for `VMDeleteArgs` River queue/unique options, worker constructor wiring, and delete execution state allow/deny coverage
   - Raised `internal/jobs` package coverage from 29.5% to 29.9% without requiring PostgreSQL or Kubernetes dependencies
   - Verified by `go test -cover -count=1 ./internal/jobs` and `go test -race -count=1 ./internal/jobs`
35. ~~**P2** — Strengthen VM delete use case namespace and duplicate-guard coverage~~ ✅ **Done** (2026-06-18)
   - Added tests for delete use case construction/audit logger injection, namespace environment lookup including disabled and missing namespaces, and pending delete duplicate detection across malformed, mismatched, approved, missing-ticket, and pending-ticket events
   - Raised `internal/usecase` package coverage from 40.0% to 44.4%; `delete_vm.go` helper coverage now includes constructor/audit injection at 100%, namespace resolution at 88.9%, and pending duplicate detection at 88.2%
   - Verified by `go test -cover -count=1 ./internal/usecase` and `go test -race -count=1 ./internal/usecase`
36. ~~**P2** — Strengthen VM delete request execution coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed tests for `DeleteVMUseCase.Execute` covering successful delete request creation, DomainEvent/Ticket atomic persistence, delete payload enrichment from service/system/cluster/owner/create snapshots, default reason generation, and AppError classification for missing VM, missing confirmation, invalid VM state, and duplicate pending delete requests
   - Raised `internal/usecase` package coverage from 44.4% to 56.1%; `delete_vm.go` `Execute` coverage is now 82.4%
   - Verified by `go test -cover -count=1 ./internal/usecase` and `go test -race -count=1 ./internal/usecase`
37. ~~**P2** — Run internal job PostgreSQL integration tests under the shared test container~~ ✅ **Done** (2026-06-18)
   - Updated the `internal/jobs` package TestMain to use the shared PostgreSQL 18 test-container bootstrap so existing DB-backed job tests no longer skip when no explicit DSN is configured locally
   - Raised `internal/jobs` package coverage from 29.9% to 60.9%; `vm_tombstone_cleanup.go` `Work` coverage rose from 8.7% to 91.3% and `hasActiveDeleteEvent` from 0.0% to 100.0%
   - Verified by `go test -cover -count=1 ./internal/jobs` and `go test -race -count=1 ./internal/jobs`
38. ~~**P2** — Run notification approval-recipient integration test under the shared test container~~ ✅ **Done** (2026-06-18)
   - Added the shared PostgreSQL 18 test-container bootstrap to `internal/notification` and removed the redundant environment-variable skip from the approval-recipient trigger test
   - Raised `internal/notification` package coverage from 41.6% to 62.9%; `OnTicketSubmitted` is now covered at 44.4% and `findApproverUserIDs` at 93.8%
   - Verified by `go test -cover -count=1 ./internal/notification` and `go test -race -count=1 ./internal/notification`
39. ~~**P2** — Run bootstrap security secret persistence tests under the shared test container~~ ✅ **Done** (2026-06-18)
   - Added the shared PostgreSQL 18 test-container bootstrap to `internal/infrastructure` and removed redundant environment-variable skips from the security bootstrap persistence tests
   - Raised `internal/infrastructure` package coverage from 24.2% to 37.0%; `ResolveBootstrapSecuritySecrets` is now covered at 86.2%, with persisted-secret load and generated-secret persistence branches exercised
   - Verified by `go test -cover -count=1 ./internal/infrastructure` and `go test -race -count=1 ./internal/infrastructure`
40. ~~**P2** — Strengthen approval cluster compatibility verdict coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed tests for `EvaluateClusterCompatibility`, `FilterCompatibleClusters`, and `EvaluateClusterPlacement` covering eligible clusters, missing policy, missing GPU capability, namespace/cluster environment mismatch, missing selected cluster, and disabled cluster verdicts
   - Aligned compatibility/placement validation with `ValidateApproval` by rejecting disabled clusters in `validateResolvedCluster`
   - Raised `internal/service` package coverage from 58.9% to 64.5%; approval cluster compatibility APIs are now covered (`FilterCompatibleClusters` 87.5%, `EvaluateClusterCompatibility` 65.7%, `EvaluateClusterPlacement` 66.7%)
   - Verified by `go test -cover -count=1 ./internal/service` and `go test -race -count=1 ./internal/service`
41. ~~**P2** — Strengthen live E2E seed database fixture coverage and rerun idempotency~~ ✅ **Done** (2026-06-18)
   - Fixed `cmd/e2e-seed` notification seeding so reruns reset/recreate both deterministic inbox notifications instead of returning after only `notif-e2e-seed-01`
   - Added PostgreSQL-backed tests for catalog/cluster policy/VM fixture idempotency, API-managed fixture skip-mode resolution diagnostics, auth provider/second-user/approval-ticket reruns, notification recreation, and batch approval projection rebuilds
   - Raised `cmd/e2e-seed` package coverage from 12.1% to 64.8%; full `make ci-go-test` profile total is now 20.0%
   - Verified by `go test -cover -count=1 ./cmd/e2e-seed`, `go test -race -count=1 ./cmd/e2e-seed`, `make ci-go-lint`, and `make ci-go-test`
42. ~~**P2** — Run bootstrap seed role/admin persistence tests under the shared test container~~ ✅ **Done** (2026-06-18)
   - Added package-level shared PostgreSQL 18 test-container bootstrap for `cmd/seed`, so DB-backed seed tests run locally without requiring a preconfigured DSN
   - Added tests for built-in role catalog reconciliation/idempotency, obsolete built-in role mapping cleanup, default admin creation/password/forced-change contract, platform-admin binding idempotency, and existing-username conflict binding
   - Raised `cmd/seed` package coverage from 2.1% to 55.8%; full `make ci-go-test` profile total is now 20.1%
   - Verified by `go test -cover -count=1 ./cmd/seed`, `go test -race -count=1 ./cmd/seed`, `make ci-go-lint`, and `make ci-go-test`
43. ~~**P2** — Strengthen live E2E seed identity-binding coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed coverage for `cmd/e2e-seed` admin/global role binding creation, idempotency, missing-role diagnostics, and system member binding role updates
   - Covered the live E2E seed path that grants the seeded admin `PlatformAdmin` and the second user system-level membership used by member-management flows
   - Raised `cmd/e2e-seed` package coverage from 64.8% to 69.0%; full `make ci-go-test` profile total remains 20.1%
   - Verified by `go test -cover -count=1 ./cmd/e2e-seed`, `go test -race -count=1 ./cmd/e2e-seed`, `make ci-go-lint`, and `make ci-go-test`
44. ~~**P2** — Strengthen ADR-0038 VM status sync worker persistence coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed worker coverage for invalid/unbound event cancellation, unassigned-cluster skip behavior, `DELETING` poll-chain stop behavior, observed K8s status/resourceVersion persistence, missing VM reconciliation to `NOT_FOUND`, and stale resourceVersion cache clearing
   - Verified the worker sends watch-cache-friendly `ListVMs` options (`metadata.name` field selector, `Limit=1`, cached `ResourceVersion`, and VMI enrichment skip) through the service/provider boundary
   - Raised `internal/jobs` package coverage from 60.9% to 66.5%; `vm_status_sync.go` `Work` coverage is now 80.0%, and full `make ci-go-test` profile total is now 20.2%
   - Verified by `go test -count=1 -run 'TestVMStatusSyncWorkerWork' -v ./internal/jobs`, `go test -cover -count=1 ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-test`
45. ~~**P2** — Strengthen scheduled directory enrichment scan worker coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL/River-backed coverage for schedule-scan args, constructor/initialization guards, empty-store scans, due scheduled enrichment job creation, River `directory_sync` enqueue payloads, duplicate suppression while a scheduled job is pending, and failed River insert job-state persistence
   - Reused provider registry extension points with a scheduled directory enrichment test adapter and removed the stale manual DSN skip from the due-evaluation test now covered by the shared PostgreSQL TestMain
   - Raised `internal/jobs` package coverage from 66.5% to 70.8%; `directory_enrichment_schedule_scan.go` `Work` coverage is now 80.0%, and full `make ci-go-test` profile total is now 20.3%
   - Verified by `go test -count=1 -run 'TestDirectoryEnrichmentScheduleScan' -v ./internal/jobs`, `go test -cover -count=1 ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-test`
46. ~~**P2** — Strengthen directory sync worker failure persistence coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed coverage for directory sync worker failure persistence when the auth provider row is missing, the auth provider adapter is unregistered, the adapter lacks directory sync support, or provider directory listing fails
   - Verified failed jobs are first marked running, then persisted as `failed` with `completed_at`, incremented `error_count`, and durable operator-facing error messages
   - Removed stale manual DSN skips from directory sync worker DB-backed tests now covered by the shared PostgreSQL TestMain
   - Raised `internal/jobs` package coverage from 70.8% to 71.8%; `directory_sync_worker.go` `failJob` coverage is now 63.6%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestDirectorySyncWorker' -v ./internal/jobs`, `go test -cover -count=1 ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-test`
47. ~~**P2** — Strengthen notification cleanup worker retention coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed coverage for the inbox notification cleanup worker deleting only notifications older than the configured retention window across multiple users
   - Verified recent and inside-retention notifications remain available while expired notifications are removed through the Ent delete path
   - Raised `internal/jobs` package coverage from 71.8% to 72.2%; `notification_cleanup.go` `Work` coverage is now 87.5%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestNotificationCleanup' -v ./internal/jobs`, `go test -cover -count=1 ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-test`
48. ~~**P2** — Strengthen ADR-0038 VM status sync self-scheduling coverage~~ ✅ **Done** (2026-06-18)
   - Added River/PostgreSQL-backed coverage for `scheduleNext` inserting `vm_status_sync` poll jobs with the dedicated queue, serialized event ID args, max attempts, future `scheduled_at`, and `scheduled` state
   - Verified duplicate scheduling for the same event does not create another active poll job and missing River schema insert failures surface as worker errors
   - Raised `internal/jobs` package coverage from 72.2% to 73.0%; `vm_status_sync.go` `scheduleNext` coverage is now 84.6%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestVMStatusSyncWorkerScheduleNext' -v ./internal/jobs`, `go test -count=1 -coverprofile=/tmp/jobs-vss.out ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `git diff --check`, and `make ci-go-test`
49. ~~**P2** — Harden VM power job duplicate-delivery idempotency~~ ✅ **Done** (2026-06-18)
   - Aligned `VMPowerArgs` with create/modify/delete job contracts by adding River `ByArgs` + `ByQueue` uniqueness for power EventID jobs on the `vm_operations` queue
   - Added a completed-event guard so duplicate delivery or retry of an already completed power event returns without executing another provider/K8s power operation or mutating the successful ticket
   - Added regression coverage for the power job insert options and duplicate completed-event skip path; `internal/jobs` package coverage rose from 73.0% to 73.2%, `vm_power.go` `Kind`/`InsertOpts` are now 100.0%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestVMPowerArgs|TestVMPowerWorker_SkipsCompletedEventWithoutExecutingProvider|TestVMPowerWorker_UsesObservedLiveStatusAfterStop|TestVMPowerWorker_SnoozesTransientClusterErrorsWithoutFailingTicket' -v ./internal/jobs`, `go test -count=1 -coverprofile=/tmp/jobs-power.out ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `git diff --check`, and `make ci-go-test`
50. ~~**P2** — Harden VM modify/delete duplicate-delivery idempotency~~ ✅ **Done** (2026-06-18)
   - Added completed-event guards for VM modify and delete workers so duplicate delivery or retry of already completed external-operation events returns before provider/K8s mutation/delete execution
   - Added PostgreSQL-backed regression coverage proving completed modify/delete events keep successful tickets and persisted VM state unchanged while skipping the external provider path
   - Added `VMModifyArgs` River job contract coverage; `internal/jobs` package coverage rose from 73.2% to 73.6%, `vm_modify.go` `Kind`/`InsertOpts` are now 100.0%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestVMModifyArgs|TestVMModifyWorker_SkipsCompletedEventWithoutExecutingProvider|TestVMDeleteWorker_SkipsCompletedEventWithoutExecutingProvider' -v ./internal/jobs`, `go test -count=1 -coverprofile=/tmp/jobs-idempotency.out ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `git diff --check`, and `make ci-go-test`
51. ~~**P2** — Strengthen VM create job contract and idempotency lookup coverage~~ ✅ **Done** (2026-06-18)
   - Added `VMCreateArgs` River job contract coverage for `vm_operations` queue routing, max attempts, and `ByArgs` + `ByQueue` uniqueness
   - Added focused coverage for the create retry idempotency lookup proving the worker uses the expected event-label selector/limit, ignores nil and unrelated candidates, returns a matched VM, and surfaces provider list failures
   - Raised `internal/jobs` package coverage from 73.6% to 74.2%; `vm_create.go` `Kind`, `InsertOpts`, and `findCreatedVMByEvent` are now 100.0%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestVMCreateArgs|TestVMCreateWorker_FindCreatedVMByEvent' -v ./internal/jobs`, `go test -count=1 -coverprofile=/tmp/jobs-create.out ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, `git diff --check`, and `make ci-go-test`
52. ~~**P2** — Strengthen VM create duplicate-delivery Work idempotency coverage~~ ✅ **Done** (2026-06-18)
   - Added PostgreSQL-backed `VMCreateWorker.Work` coverage proving a retry with an existing event-labeled VM skips K8s create and completes the event/ticket from the already-created VM
   - Verified the idempotency lookup keeps the expected event-label selector/limit and that the provider `CreateVM` path is not called after partial success
   - Raised `internal/jobs` package coverage from 74.2% to 75.0%; `vm_create.go` `Work` coverage is now 56.5%, and full `make ci-go-test` profile total remains 20.3%
   - Verified by `go test -count=1 -run 'TestVMCreateWorker_SkipsK8sCreateWhenEventLabeledVMAlreadyExists|TestVMCreateWorker_FindCreatedVMByEvent|TestVMCreateArgs' -v ./internal/jobs`, `go test -count=1 -coverprofile=/tmp/jobs-create-work.out ./internal/jobs`, `go test -race -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-test`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
53. ~~**P2** — Harden VMService create `AlreadyExists` idempotency~~ ✅ **Done** (2026-06-18)
   - `ExecuteK8sCreate` now detects Kubernetes `AlreadyExists`, reads the existing VM by name, and treats the create as idempotent only when the existing VM has the same `shepherd.io/event-id` label as the requested spec
   - Mismatched ownership labels and failed existing-VM lookups remain errors, with `apierrors.IsAlreadyExists` still preserved for callers inspecting the wrapped error
   - Raised `internal/service` package coverage from 64.5% to 64.7%; `vm_service.go` `ExecuteK8sCreate` and `existingVMForAlreadyExistsCreate` are now covered at 76.5%, and full `make ci-go-test` profile total is now 20.4%
   - Verified by `go test -count=1 -run 'TestVMServiceExecuteK8sCreate' -v ./internal/service`, `go test -count=1 -coverprofile=/tmp/service-alreadyexists.out ./internal/service`, `go test -race -count=1 ./internal/service`, `make ci-go-lint`, and `make ci-go-test`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
54. ~~**P2** — Strengthen VMService provider-boundary coverage~~ ✅ **Done** (2026-06-18)
   - Added VMService unit coverage for provider read/list/manifest calls, SSA update validation/rendering/error wrapping, VM mutation dry-run/execution validation/error wrapping, and start/stop/restart/delete delegation
   - Converted the existing namespace-provisioning provider stub into a broader service-boundary test double that records provider calls and supports controlled provider failures without HTTP or real Kubernetes dependencies
   - Raised `internal/service` package coverage from 64.7% to 67.3%; `vm_service.go` `GetVM`, `ListVMs`, `StartVM`, `StopVM`, `RestartVM`, and `DeleteVM` are now 100.0%, `ExecuteK8sUpdate` is 93.3%, `DryRunVMMutation` is 91.7%, and `ExecuteVMMutation` is 92.9%
   - Verified by `go test -count=1 -run 'TestVMServiceProviderReadMethods|TestVMServiceGetVMManifestYAML|TestVMServiceExecuteK8sUpdate|TestVMServiceVMMutationMethods|TestVMServicePowerAndDeleteMethods' -v ./internal/service`, `go test -count=1 -coverprofile=/tmp/service-boundary.out ./internal/service`, `go test -race -count=1 ./internal/service`, `make ci-go-lint`, and `make ci-go-test`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
55. ~~**P2** — Harden VMService unconfigured-provider failure behavior~~ ✅ **Done** (2026-06-18)
   - Centralized VMService infrastructure-provider configuration checks behind `infrastructureProvider()` so nil receivers and `NewVMService(nil)` return a stable configuration error instead of panicking
   - Applied the guard consistently across read/list/manifest, create/update, mutation dry-run/execution, storage-profile lookup, power/delete, and console-stream methods
   - Raised `internal/service` package coverage from 67.3% to 68.1%; `vm_service.go` `infrastructureProvider` is now 100.0%, `ExecuteK8sCreate` is 82.4%, and full `make ci-go-test` profile total is now 20.5%
   - Verified by `go test -count=1 -run 'TestVMServiceMethods_ReturnConfiguredProviderError|TestVMServiceExecuteK8sUpdate' -v ./internal/service`, `go test -count=1 -coverprofile=/tmp/service-config-guard.out ./internal/service`, `go test -race -count=1 ./internal/service`, `make ci-go-lint`, and `make ci-go-test`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
56. ~~**P2** — Strengthen DB-backed API VM create request coverage~~ ✅ **Done** (2026-06-18)
   - Added `CreateVMRequest` handler coverage proving a valid `/vms/request` submission returns `202 Accepted`, writes a pending `CREATE` ticket, writes a pending `VM_CREATION_REQUESTED` domain event, preserves resource target overrides in the event payload, and does not create a VM row before approval
   - Seeded real service/template/instance-size/namespace catalog prerequisites through PostgreSQL, using a requestable persistent-root template source so the test exercises the production create request contract instead of a validation failure path
   - `internal/api/handlers` package coverage is 63.1%; full `make ci-go-test` profile total remains 20.5%
   - Verified by `go test -count=1 -run 'TestCreateVMRequest_CreatesPendingCreateTicket|TestCreateVMUseCase_PersistsResolvedTargetResources' -v ./internal/api/handlers ./internal/usecase`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-create.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestCreateVMRequest_CreatesPendingCreateTicket' ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
57. ~~**P2** — Strengthen DB-backed API VM delete request coverage~~ ✅ **Done** (2026-06-18)
   - Added `DeleteVM` handler coverage proving a valid prod-namespace `DELETE /vms/{vm_id}` request with `confirm_name` returns `202 Accepted`, echoes event/ticket IDs, writes a pending `DELETE` ticket, writes a pending `VM_DELETION_REQUESTED` domain event, preserves actor/scope/status payload fields, and leaves the VM row unchanged before approval
   - Seeded real namespace, cluster, service, and stopped VM prerequisites through PostgreSQL so the handler test exercises authorization, namespace confirmation, and delete use-case wiring without mocking the request path
   - Closed the Phase 3 DB-backed `/api/v1/vms` CRUD handler success-path checklist; `internal/api/handlers` package coverage is now 63.2%, and full `make ci-go-test` profile total remains 20.5%
   - Verified by `go test -count=1 -run 'TestDeleteVM_CreatesPendingDeleteTicket|TestDeleteVMUseCaseExecuteCreatesDeleteRequest' -v ./internal/api/handlers ./internal/usecase`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-delete.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestDeleteVM_CreatesPendingDeleteTicket' ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
58. ~~**P2** — Strengthen VMService end-to-end create approval/worker coverage~~ ✅ **Done** (2026-06-18)
   - Added DB-backed ticketing integration coverage that submits a `CreateVMUseCase` request, approves it through the real ticketing service and `ApprovalAtomicWriter`, verifies `vm_create` and `vm_status_sync` River jobs, runs `VMCreateWorker`, and proves VMService reaches the provider create boundary
   - Validates final `DomainEvent` `COMPLETED`, `Ticket` `SUCCESS`, VM row status/name/resource/storage-class fields, and generated provider VM labels/scope through a shared PostgreSQL + River schema bootstrap
   - Closed the Phase 3 VMService end-to-end checklist; `internal/governance/ticketing` package coverage is now 60.3%, and full `make ci-go-test` profile total remains 20.5%
   - Verified by `go test -count=1 -run 'TestVMServiceEndToEnd_CreateRequestApprovalAndWorker' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `go test -count=1 -coverprofile=/tmp/ticketing-vmservice-e2e.out ./internal/governance/ticketing`, `go test -race -count=1 -run 'TestVMServiceEndToEnd_CreateRequestApprovalAndWorker' ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
59. ~~**P2** — Implement KubeVirt instance type and preference catalog reads~~ ✅ **Done** (2026-06-18)
   - Added a narrow provider catalog client for namespace-scoped `VirtualMachineInstancetype` / `VirtualMachinePreference` and cluster-scoped `VirtualMachineClusterInstancetype` / `VirtualMachineClusterPreference` list operations, backed by KubeVirt client-go v1.8.4
   - Implemented KubeVirtProvider domain mapping for instance type CPU/memory and preference names, plus MockProvider seed/list support so tests can exercise the same catalog contract without a live cluster
   - Closed the Phase 2 instance type/preference checklist; `internal/provider` package coverage is now 51.2%, and full `make ci-go-test` profile total is now 20.6%
   - Verified by `go test -count=1 -run 'TestKubeVirtProvider_ListInstanceTypeCatalog|TestMockProvider_InstanceTypeCatalog' -v ./internal/provider`, `go test -count=1 ./internal/provider`, `go test -count=1 -coverprofile=/tmp/provider-instancetype.out ./internal/provider`, `go test -race -count=1 -run 'TestKubeVirtProvider_ListInstanceTypeCatalog|TestMockProvider_InstanceTypeCatalog' ./internal/provider`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
60. ~~**P2** — Implement label-based pending adoption discovery~~ ✅ **Done** (2026-06-18)
   - Added shared Shepherd VM label constants and wired create/dry-run/idempotency paths to use the same `shepherd.io/service-id`, `shepherd.io/template-id`, and `shepherd.io/event-id` keys
   - Added a DB-backed adoption discovery service that lists K8s VMs by `shepherd.io/service-id` label existence, skips existing VM rows and missing Service references, creates new `PENDING` `pending_adoptions`, refreshes existing pending rows, and leaves `ADOPTED`/`REJECTED`/`EXPIRED` rows unchanged
   - Closed the Phase 2 label-based discovery checklist item; `internal/service` package coverage is now 68.4%, and full `make ci-go-test` profile total is now 20.7%
   - Verified by `go test -count=1 -run 'TestAdoptionDiscoveryService_DiscoversLabelledVMs|TestVMCreateWorker_FindCreatedVMByEvent|TestVMServiceExecuteK8sCreate' -v ./internal/service ./internal/jobs`, `go test -count=1 ./internal/service`, `go test -count=1 -coverprofile=/tmp/service-adoption-discovery.out ./internal/service`, `go test -race -count=1 -run 'TestAdoptionDiscoveryService_DiscoversLabelledVMs' ./internal/service`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
61. ~~**P2** — Configure periodic pending adoption scan~~ ✅ **Done** (2026-06-18)
   - Added a `vm_adoption_discovery_scan` River worker that scans enabled healthy clusters against enabled same-environment namespaces and invokes the label-based adoption discovery service with the system scanner actor
   - Registered the scan worker in the VM module and configured a 5-minute River periodic job with startup execution and period/queue/args uniqueness
   - Closed the Phase 2 periodic adoption scan checklist item; `internal/jobs` package coverage is now 75.4%, and full `make ci-go-test` profile total remains 20.7%
   - Verified by `go test -count=1 -run 'TestVMAdoptionDiscoveryScan|TestVMModule_RegisterWorkers_RegistersVMLifecycleWorkers|TestRegisterPeriodicJobs_WiringContract' -v ./internal/jobs ./internal/app/modules ./internal/app`, `go test -count=1 ./internal/jobs ./internal/app/modules ./internal/app`, `go test -count=1 -coverprofile=/tmp/jobs-adoption-scan.out ./internal/jobs`, `go test -race -count=1 -run 'TestVMAdoptionDiscoveryScanWorker' ./internal/jobs`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
62. ~~**P2** — Implement pending adoption admin list/reject baseline~~ ✅ **Done** (2026-06-18)
   - Added OpenAPI and generated Go/TypeScript contracts for `GET /admin/pending-adoptions` and `POST /admin/pending-adoptions/{pending_adoption_id}/reject`
   - Added platform-admin-only handlers that filter and paginate `pending_adoptions`, reject only `PENDING` rows, and write `adoption.rejected` audit logs with optional operator reason
   - Closed the Phase 2 admin list/reject baseline; adoption execution into VM DB records was still deferred at this checkpoint and is closed by item 63 below, `internal/api/handlers` package coverage was 63.3%, and full `make ci-go-test` profile total was 20.8%
   - Verified by `go test -count=1 -run 'TestListPendingAdoptions|TestRejectPendingAdoption|TestPendingAdoptionAdminHandlers' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-adoption.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestRejectPendingAdoption_RejectsPendingRowAndAudits' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
63. ~~**P2** — Implement pending adoption admin adopt execution~~ ✅ **Done** (2026-06-18)
   - Added OpenAPI and generated Go/TypeScript contracts for `POST /admin/pending-adoptions/{pending_adoption_id}/adopt`, returning the updated adoption candidate plus adopted VM id/name
   - Added a platform-admin-only adopt handler that re-reads the live K8s VM through `VMService`, validates Shepherd service ownership, rejects stale/non-pending/duplicate candidates, and transactionally creates the DB VM inventory row while marking the `pending_adoptions` row `ADOPTED`
   - Added `adoption.adopted` audit logging with VM/service/resource details and optional operator reason; this closes the Phase 2 Resource Adoption Admin API and audit checklist items, `internal/api/handlers` package coverage is now 63.2%, and full `make ci-go-test` profile total remains 20.8%
   - Verified by `go test -count=1 -run 'TestListPendingAdoptions|TestRejectPendingAdoption|TestAdoptPendingAdoption|TestPendingAdoptionAdminHandlers' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-adoption-adopt.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestAdoptPendingAdoption_AdoptsLiveVMAndAudits' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
64. ~~**P2** — Close User My Requests API security evidence and ticket filter validation~~ ✅ **Done** (2026-06-18)
   - Confirmed `GET /tickets?mine=true` is the V1 My Requests API: authenticated users can list only their own tickets without `ticket:view`, while all-ticket listing still requires `ticket:view`
   - Added explicit ticket list filter enum validation for `status`, `status_group`, `operation_type`, and `placement_snapshot`, returning `400 INVALID_REQUEST` instead of silently returning empty lists for invalid filters
   - Closed the Phase 4 User View - My Requests checklist item; `internal/api/handlers` package coverage is now 63.3%, and full `make ci-go-test` profile total is now 20.9%
   - Verified by `go test -count=1 -run 'TestListApprovals_MineFiltersByRequesterWithoutApprovalViewPermission|TestListTickets_RequiresViewPermissionUnlessMine|TestListTickets_RejectsInvalidFilterEnums|TestListApprovals_StatusGroupFiltersActiveAndTerminalTickets' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-my-requests.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestListApprovals_MineFiltersByRequesterWithoutApprovalViewPermission|TestListTickets_RequiresViewPermissionUnlessMine|TestListTickets_RejectsInvalidFilterEnums' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
65. ~~**P2** — Harden remaining enum query filters and list sort-order behavior~~ ✅ **Done** (2026-06-18)
   - Added shared handler-side enum query validation for optional oapi-codegen query parameters that otherwise bind as string-like values: audit `category`, namespace `environment`, cluster `selected_dv_volume_mode`, and system/VM/batch `sort_order`
   - Made `GET /systems?sort_order=asc` and `GET /vms?sort_order=asc` honor explicit ascending created-time order while preserving the existing newest-first default for omitted sort order
   - Added regression tests for invalid enum values returning `400 INVALID_REQUEST` and for explicit ascending list order; `internal/api/handlers` package coverage is now 63.4%, and full `make ci-go-test` profile total remains 20.9%
   - Verified by `go test -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortOrder|TestSystemHandler_ListSystems_SortsByCreatedAtAscendingWhenRequested|TestListAuditLogs_RejectsInvalidCategory|TestListNamespaces_RejectsInvalidEnvironment|TestListClusters_RejectsInvalidSelectedDVVolumeMode|TestListVMs_RejectsInvalidSortOrder|TestListVMs_SortsByCreatedAtAscendingWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortOrder' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-enum-query.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortOrder|TestSystemHandler_ListSystems_SortsByCreatedAtAscendingWhenRequested|TestListAuditLogs_RejectsInvalidCategory|TestListNamespaces_RejectsInvalidEnvironment|TestListClusters_RejectsInvalidSelectedDVVolumeMode|TestListVMs_RejectsInvalidSortOrder|TestListVMs_SortsByCreatedAtAscendingWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortOrder' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
66. ~~**P2** — Type and validate pending adoption resource-type filters~~ ✅ **Done** (2026-06-18)
   - Added `PendingAdoptionResourceType` to the OpenAPI contract and generated Go/TypeScript types so V1 pending-adoption list responses and `resource_type` query filters explicitly model the only supported adoption resource kind: `VirtualMachine`
   - Added handler-side validation for `GET /admin/pending-adoptions?resource_type=...`, returning `400 INVALID_REQUEST` for unsupported resource kinds instead of silently returning an empty list
   - Added regression coverage for invalid pending adoption `resource_type`; this keeps the Phase 2 adoption management API aligned with the V1 VM-only adoption execution path, `internal/api/handlers` package coverage remains 63.4%, and full `make ci-go-test` profile total remains 20.9%
   - Verified by `go test -count=1 -run 'TestListPendingAdoptions' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-pending-adoption-resource-type.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestListPendingAdoptions' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`
67. ~~**P2** — Enforce and implement list `sort_by` fields~~ ✅ **Done** (2026-06-18)
   - Added whitelist validation for `sort_by` on `GET /systems`, `GET /vms`, and `GET /vms/batch`, returning `400 INVALID_REQUEST` for unsupported fields instead of silently ignoring the parameter
   - Implemented endpoint-specific field sorting while preserving existing default `created_at desc`: systems support `created_at`, `name`, and `created_by`; VMs support `created_at`, `name`, `namespace`, and `status`; VM batches support `created_at`, `status`, `child_count`, `success_count`, `failed_count`, and `pending_count`
   - Added regression tests for invalid `sort_by` values and non-default field ordering; `internal/api/handlers` package coverage is now 63.5%, and full `make ci-go-test` profile total remains 20.9%
   - Verified by `go test -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortBy|TestSystemHandler_ListSystems_SortsByNameWhenRequested|TestListVMs_RejectsInvalidSortBy|TestListVMs_SortsByNameWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortBy|TestBatchHandler_ListVMBatches_SortsByFailedCountWhenRequested' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-sort-by.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortBy|TestSystemHandler_ListSystems_SortsByNameWhenRequested|TestListVMs_RejectsInvalidSortBy|TestListVMs_SortsByNameWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortBy|TestBatchHandler_ListVMBatches_SortsByFailedCountWhenRequested' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` still fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
68. ~~**P2** — Type and validate admin template enum query filters~~ ✅ **Done** (2026-06-18)
   - Tightened `GET /admin/templates` OpenAPI query parameters so `source_type` is typed as `containerdisk | cdi_image_import | cdi_pvc_clone` and `catalog_scope` is typed as `unclassified | test | prod | all` in generated Go and TypeScript clients
   - Added handler-side whitelist validation after normalizing case/whitespace, returning `400 INVALID_REQUEST` for unsupported `source_type` or `catalog_scope` instead of silently returning an empty filtered list
   - Added regression tests for invalid enum filters and valid normalized enum casing; `internal/api/handlers` package coverage remains 63.5%, and full `make ci-go-test` profile total remains 20.9%
   - Verified by `go test -count=1 -run 'TestListAdminTemplates_SupportsQuickSearchAndExactFilters|TestListAdminTemplates_RejectsInvalidEnumFilters' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-admin-template-filter.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestListAdminTemplates_SupportsQuickSearchAndExactFilters|TestListAdminTemplates_RejectsInvalidEnumFilters' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` still fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
69. ~~**P2** — Type and validate VM status query filter~~ ✅ **Done** (2026-06-18)
   - Tightened `GET /vms?status=...` in OpenAPI so generated Go and TypeScript clients expose the VM lifecycle status enum instead of a plain string
   - Added handler-side whitelist validation that returns `400 INVALID_REQUEST` for unsupported VM statuses instead of silently returning an empty list; normalized whitespace is applied before the existing DB filter and `DELETING` tombstone exception logic
   - Added regression tests for invalid VM status filters and valid `RUNNING` status filtering; `internal/api/handlers` package coverage remains 63.5%, and full `make ci-go-test` profile total remains 20.9%
   - Verified by `go test -count=1 -run 'TestListVMs_RejectsInvalidStatus|TestListVMs_FiltersByStatusWhenRequested|TestListVMs_SupportsExactAdvancedFilters' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-vm-status-filter.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestListVMs_RejectsInvalidStatus|TestListVMs_FiltersByStatusWhenRequested|TestListVMs_SupportsExactAdvancedFilters' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` still fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
70. ~~**P2** — Type and validate cluster DataVolume access-mode query filters~~ ✅ **Done** (2026-06-19)
   - Tightened `GET /admin/clusters?selected_dv_access_modes=...` in OpenAPI so generated Go and TypeScript clients expose the Kubernetes PVC access-mode enum (`ReadWriteOnce`, `ReadOnlyMany`, `ReadWriteMany`, `ReadWriteOncePod`) instead of a plain string array
   - Added handler-side per-value whitelist validation after trimming empty/whitespace-only values, returning `400 INVALID_REQUEST` for unsupported access modes before cluster compatibility evaluation
   - Added regression coverage for invalid access-mode filters and kept explicit root-volume compatibility filter coverage aligned with the generated enum type; `internal/api/handlers` package coverage remains 63.5%, and full `make ci-go-test` profile total is now 21.0%
   - Verified by `go test -count=1 -run 'TestBuildClusterCompatibilityFilter_IncludesExplicitRootVolumeMode|TestListClusters_RejectsInvalidSelectedDVVolumeMode|TestListClusters_RejectsInvalidSelectedDVAccessMode|TestListClusters_CreateCompatibilityFilterMarksExplicitStorageClassRequirement' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-cluster-dv-access-mode.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestBuildClusterCompatibilityFilter_IncludesExplicitRootVolumeMode|TestListClusters_RejectsInvalidSelectedDVVolumeMode|TestListClusters_RejectsInvalidSelectedDVAccessMode|TestListClusters_CreateCompatibilityFilterMarksExplicitStorageClassRequirement' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` still fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
71. ~~**P2** — Type list `sort_by` query parameters per endpoint~~ ✅ **Done** (2026-06-19)
   - Replaced the generic OpenAPI `SortBy` query parameter on `GET /systems`, `GET /vms`, and `GET /vms/batch` with endpoint-specific enum parameters so generated Go and TypeScript clients expose the same allowed fields enforced by the handlers
   - Generated types now model systems `sort_by` as `created_at | name | created_by`, VMs as `created_at | name | namespace | status`, and VM batches as `created_at | status | child_count | success_count | failed_count | pending_count`
   - Kept existing runtime validation and sorting behavior unchanged while casting the generated enum types explicitly at the handler boundary; `internal/api/handlers` package coverage remains 63.5%, and full `make ci-go-test` profile total remains 21.0%
   - Verified by `go test -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortBy|TestSystemHandler_ListSystems_SortsByCreatedAtAscendingWhenRequested|TestSystemHandler_ListSystems_SortsByNameWhenRequested|TestListVMs_RejectsInvalidSortBy|TestListVMs_SortsByCreatedAtAscendingWhenRequested|TestListVMs_SortsByNameWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortBy|TestBatchHandler_ListVMBatches_SortsByFailedCountWhenRequested' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 -coverprofile=/tmp/api-handlers-sort-by-contract.out ./internal/api/handlers`, `go test -race -count=1 -run 'TestSystemHandler_ListSystems_RejectsInvalidSortBy|TestSystemHandler_ListSystems_SortsByCreatedAtAscendingWhenRequested|TestSystemHandler_ListSystems_SortsByNameWhenRequested|TestListVMs_RejectsInvalidSortBy|TestListVMs_SortsByCreatedAtAscendingWhenRequested|TestListVMs_SortsByNameWhenRequested|TestBatchHandler_ListVMBatches_RejectsInvalidSortBy|TestBatchHandler_ListVMBatches_SortsByFailedCountWhenRequested' ./internal/api/handlers`, `make api-check`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; `make ci-live-e2e-latest-evidence` still fails because no full live E2E evidence manifest exists under `.run/live-e2e`, and `make live-e2e-readiness` still fails because no real kubeconfig is configured (`E2E_KUBECONFIG_B64` or `k8s-admin.yaml`)
72. ~~**P2** — Enforce Go coverage threshold on handwritten code in CI~~ ✅ **Done** (2026-06-19)
   - Added `docs/design/ci/scripts/check_go_coverage_threshold.sh` and wired `make ci-go-test` to run it after generating the complete race `coverage.out` profile
   - The blocking threshold is ≥60% over handwritten Go statements; generated Ent/OpenAPI/spec-embed code and plugin example templates remain in the raw artifact but are excluded from the pass/fail percentage
   - Current included coverage is 62.8% (`15340/24442` statements across 199 files); the complete raw `coverage.out` total remains 21.0% because it still includes generated/template code for artifact inspection
   - Verified by `bash docs/design/ci/scripts/check_go_coverage_threshold.sh --profile coverage.out --min 60`, expected-failure `bash docs/design/ci/scripts/check_go_coverage_threshold.sh --profile coverage.out --min 99`, `make ci-go-coverage-threshold`, `make ci-go-test`, `bash docs/design/ci/scripts/check_no_new_run_scripts.sh`, `make ci-go-lint`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; live E2E remains blocked as above by missing full evidence and missing real kubeconfig
73. ~~**P2** — Ship admin pending-adoption frontend workflow~~ ✅ **Done** (2026-06-19)
   - Added the platform-admin `/admin/pending-adoptions` page with summary metrics, quick search, advanced status/cluster/namespace filters, a paginated candidate table, and adopt/reject decision modals with optional audit reasons
   - Wired the generated OpenAPI client to `GET /admin/pending-adoptions`, `POST /admin/pending-adoptions/{pending_adoption_id}/adopt`, and `POST /admin/pending-adoptions/{pending_adoption_id}/reject`, then exposed the route in the admin menu behind `platform:admin`
   - Closed the governance tail failures found during verification by keeping frontend enum filters aligned with generated OpenAPI types, moving public email fixtures to reserved domains, and renaming a schema-diff test field that triggered a `gitleaks` generic-key false positive
   - Verified by `npm run test:run --prefix web -- AdminPendingAdoptionsContent.test.tsx appLayoutRoutes.test.tsx`, targeted regression `npm run test:run --prefix web -- AdminPendingAdoptionsContent.test.tsx appLayoutRoutes.test.tsx AdminTemplatesContent.test.tsx useVMManagementController.test.tsx AdminApprovalsContent.test.tsx`, `npm run typecheck --prefix web`, `npm run lint --prefix web`, `node docs/design/ci/scripts/frontend_api_ast_scan.mjs web/src`, `go test -count=1 ./cmd/seed ./cmd/e2e-seed`, `go test -count=1 ./cmd/kubevirt-schema-diff`, `make public-hygiene-scan`, `make secrets-scan`, and `make ci-governance`; this does not complete a real-cluster live E2E run, which still depends on live readiness inputs
74. ~~**P2** — Prevent stale deferred status for pending-adoption review UI~~ ✅ **Done** (2026-06-19)
   - Updated `DEFERRED_FOLLOWUPS.md` so the completed `/admin/pending-adoptions` review UI is no longer described as future scope; remaining deferred work is now limited to bulk decisions, richer operator workflows, and broader adoption policy
   - Added a design governance guard that fails if the deferred follow-up document reintroduces the stale "dedicated review UI" claim after the review page has shipped
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `rg -n "dedicated review UI|Productized resource adoption" docs/design/DEFERRED_FOLLOWUPS.md docs/design/ci/scripts/check_design_doc_governance.sh`
75. ~~**P2** — Keep frontend route-count and adoption compensation docs synced with implementation~~ ✅ **Done** (2026-06-19)
   - Updated current-state, Phase 5, and checklist docs from 29 to the actual 32 App Router `page.tsx` files under `web/src/app`, including the new pending-adoptions route
   - Updated Phase 4 reconciler/adoption wording so `pending_adoptions` is no longer described as schema-only now that discovery, platform-admin API, and review UI workflow exist
   - Added design governance checks that compare documented App Router page counts with the filesystem and reject `pending_adoptions` schema-only claims
   - Verified by `find web/src/app -type f -name 'page.tsx' | wc -l`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, stale-doc searches for the old App Router count and adoption schema-only wording, and `git diff --check`
76. ~~**P2** — Keep documented OpenAPI operation counts synced with the contract~~ ✅ **Done** (2026-06-19)
   - Updated current-state, Phase 1, Phase 4, Phase 5, README, and checklist docs to the current 140 `operationId`s in `api/openapi.yaml`
   - Added a design governance check that counts `operationId:` entries in the canonical OpenAPI spec and fails if key summary docs carry a stale endpoint/operation count
   - Verified by `rg -n "operationId:" api/openapi.yaml | wc -l`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, stale-doc search for old/new operation counts, and `git diff --check`
77. ~~**P2** — Keep documented Ent schema counts synced with the filesystem~~ ✅ **Done** (2026-06-19)
   - Added a design governance check that counts `ent/schema/*.go` files and fails if key summary docs carry a stale Ent schema count
   - Verified by `find ent/schema -maxdepth 1 -type f -name '*.go' -print | wc -l`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make ci-governance`, and `git diff --check`
78. ~~**P2** — Prevent stale docs from asking to remove VM `PENDING`~~ ✅ **Done** (2026-06-19)
   - Updated Phase 2, Phase 4, and the global checklist to reflect the current status model: pre-approval VM requests create pending Ticket/DomainEvent records without a VM row, while VM `PENDING` remains a K8s/KubeVirt scheduler-wait state
   - Added a design governance guard that rejects the obsolete VM `PENDING` removal claim outside accepted ADR history
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, stale-doc searches for the old VM `PENDING` removal wording, `make ci-governance`, and `git diff --check`
79. ~~**P2** — Re-verify frontend full local lane after admin pending-adoption route~~ ✅ **Done** (2026-06-19)
   - Re-ran the full frontend local lane after adding `/admin/pending-adoptions`, covering ESLint, Next route type generation/typecheck, Vitest local shards, and the Next.js production build
   - The production build generated all 32 App Router pages and listed `/admin/pending-adoptions` in the route output
   - Verified by `make ci-frontend-unit-local`; live E2E remains separate and still requires real-cluster inputs plus a full evidence manifest
80. ~~**P2** — Restore frontend dependency/dead-code hygiene after advisory drift~~ ✅ **Done** (2026-06-19)
   - Updated the frontend lockfile so transitive `undici` resolves to the non-vulnerable audit target, preserving `package.json` dependency ranges
   - Removed the unused exported `PendingAdoptionResourceType` type from the admin pending-adoptions feature surface while keeping the internal type annotation for the exported resource-type constant
   - Verified by `make ci-frontend-deadcode`, `npm run typecheck --prefix web`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`
81. ~~**P2** — Restore API sync lane after intentional query enum hardening~~ ✅ **Done** (2026-06-19)
   - Added controlled `oasdiff` breaking-change ignores for the intentional query enum restrictions on admin template filters, VM filters, and list `sort_by` parameters
   - This preserves the stricter generated client types and handler-side `400 INVALID_REQUEST` validation while allowing the API breaking-change gate to pass with documented intent
   - Verified by `make ci-api-sync-local`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`
82. ~~**P2** — Re-verify backend lane after Go/API/provider changes~~ ✅ **Done** (2026-06-19)
   - Re-ran the backend CI lane, covering govulncheck, custom `golangci-lint` with `shepherd-arch`, race-enabled Go tests with atomic coverage, handwritten coverage threshold enforcement, Go build, and Docker-PostgreSQL-backed master-flow backend behavior suites
   - Included Go coverage remains 62.8% over handwritten code, above the 60.0% enforced threshold
   - Verified by `make ci-backend`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`
83. ~~**P2** — Re-verify mock E2E smoke after frontend/API changes~~ ✅ **Done** (2026-06-19)
   - Re-ran the Playwright `smoke-chromium` project through the local `ci-e2e-smoke` target after the pending-adoptions frontend, API enum typing, and dependency hygiene changes
   - The mock E2E lane built all 32 App Router pages, listed `/admin/pending-adoptions`, and passed all 40 smoke tests
   - Verified by `make ci-e2e-smoke`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`; real live E2E remains separate and still requires real-cluster inputs plus full evidence
84. ~~**P2** — Re-run sequential PR CI bundle after lane fixes~~ ✅ **Done** (2026-06-19)
   - Re-ran the workflow-equivalent local PR bundle serially after API breaking-ignore, backend, governance, frontend, and mock E2E fixes
   - Covered `ci-prep`, `ci-api-sync-local`, `ci-backend`, `ci-governance`, `ci-frontend`, and `ci-e2e-smoke`; this remains separate from real live E2E release evidence
   - Verified by `make pr-sequential`; real live E2E is still blocked without real-cluster inputs plus a full evidence manifest
85. ~~**P2** — Close current-state drift for shipped resource adoption workflow~~ ✅ **Done** (2026-06-19)
   - Updated `CURRENT_STATE.md` so the implementation snapshot includes the VM adoption discovery scan worker, `pending_adoptions` persistence, platform-admin list/reject/adopt endpoints, and `/admin/pending-adoptions` review UI
   - Added design-governance assertions so current-state docs cannot omit the shipped adoption workflow after the backend/API/frontend work
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
86. ~~**P2** — Close stale admin-approval overcommit-warning UX priority drift~~ ✅ **Done** (2026-06-19)
   - Reconciled the top-level master-flow status with the shipped admin approval UI and phase checklist evidence for production overcommit review warnings
   - Added design-governance assertions so `CHECKLIST.md` cannot keep listing prod overcommit warning UX as an open P3 item after the frontend warning and regression test exist
   - Verified by `npm run test:run --prefix web -- AdminApprovalsContent.test.tsx`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `git diff --check`
87. ~~**P2** — Close stale Stage 5.E/5.F master-flow priority drift~~ ✅ **Done** (2026-06-19)
   - Reconciled the top-level master-flow status table with the phase-4 evidence showing Stage 5.E batch operations complete and Stage 5.F V1 inbox notifications complete
   - Added design-governance assertions so `CHECKLIST.md` cannot list the completed batch baseline or V1 notification baseline as open priority items
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
88. ~~**P3** — Harden provider-side VM create idempotency before SSA apply~~ ✅ **Done** (2026-06-19)
   - Added a KubeVirt provider create guard that reads an existing same-name VM before SSA apply and only treats it as idempotent when the `shepherd.io/event-id` ownership label matches the requested create event
   - Rejects unowned same-name VMs with a wrapped Kubernetes `AlreadyExists` error instead of allowing create-style SSA to patch an unrelated VM
   - Verified by `go test -count=1 -run 'TestKubeVirtProviderCreateVM_' -v ./internal/provider`, `go test -count=1 ./internal/provider`, `go test -count=1 -run 'TestVMServiceExecuteK8sCreate_' -v ./internal/service`, `make ci-go-lint`, `make ci-go-test`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
89. ~~**P2** — Close remaining Stage 6/phase-4 master-flow status drift~~ ✅ **Done** (2026-06-19)
   - Reconciled the top-level and phase-4 master-flow status tables so Stage 6 reflects the shipped V1 console baseline instead of treating noVNC proxy internals and VNC active revocation as current-scope P2 debt
   - Kept live E2E release evidence separate: the real-cluster run still requires `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` plus a full `.run/live-e2e` evidence manifest
   - Verified by `make live-e2e-readiness` (expected failure: missing real kubeconfig only after static gates passed), `make ci-live-e2e-latest-evidence` (expected failure: no full release evidence manifest), `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
90. ~~**P2** — Close stale Phase 1 API contract-first checklist gates~~ ✅ **Done** (2026-06-19)
   - Reconciled Phase 1 checklist status for OpenAPI 3.1 compat generation, `api-check` generated-sync enforcement, and ADR-0029 libopenapi StrictMode workflow coverage
   - Added design-governance assertions so the shipped API contract-first CI gates cannot regress to unchecked checklist work while the workflow, Makefile, compat artifact, and runtime validator remain wired
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
91. ~~**P2** — Close stale Phase 1 transaction/test-infrastructure/vanity-path checklist drift~~ ✅ **Done** (2026-06-19)
   - Reconciled Phase 1 checklist status for ADR-0012 transaction management, the shared PostgreSQL-backed test infrastructure, and vanity Go module/internal import path usage
   - Added design-governance assertions covering `go.mod`, legacy non-vanity internal imports, shared DB test helpers, ADR-0012 sqlc/ValidateSpec gates, and the `txboundary` analyzer wiring
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
92. ~~**P2** — Enforce Phase 1 Ent schema definition standards~~ ✅ **Done** (2026-06-19)
   - Added design-governance checks for concrete Ent schemas: immutable unique string `id`, shared timestamp mixin usage, and no direct `created_at`/`updated_at` field duplication outside `mixin.go`
   - Reconciled Phase 1 checklist/status text so schema standards are no longer listed as open current-scope work
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
93. ~~**P2** — Clarify Phase 2 snapshot/clone/migration provider scope~~ ✅ **Done** (2026-06-19)
   - Marked unchecked snapshot, full clone, and live migration provider methods as RFC-backed future scope in the Phase 2 checklist, matching `CURRENT_STATE.md`, `DEFERRED_FOLLOWUPS.md`, and RFC-0012/0013/0014
   - Added a design-governance assertion so those methods cannot reappear as unqualified current V1 provider work
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
94. ~~**P2** — Enforce ADR-0028 optional pointer-use audit~~ ✅ **Done** (2026-06-19)
   - Extended `check_openapi_critical_contract.go` to freeze the generated optional pointer exception set and reject new optional pointer fields unless they use `omitzero` value semantics or an explicit ADR-0028 exception
   - Reconciled Phase 1 checklist/status text so optional pointer-use audit is no longer listed as open current-scope work
   - Verified by `go run docs/design/ci/scripts/check_openapi_critical_contract.go`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
95. ~~**P2** — Close stale Phase 1 progress-tracking summary drift~~ ✅ **Done** (2026-06-19)
   - Reconciled the `CHECKLIST.md` Progress Tracking Phase 1 row with the authoritative top-level Phase 1 status after Ent schema standards, ADR-0012/test infrastructure, and ADR-0028 pointer-audit gates were closed
   - Added design-governance assertions so the legacy Phase 1 percentage and pre-audit standards summary cannot return
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
96. ~~**P2** — Close stale Phase 2 provider timeout and test-lane checklist drift~~ ✅ **Done** (2026-06-19)
   - Marked Phase 2 provider context timeout handling complete based on bounded `k8s.operation_timeout` contexts in KubeVirt provider operations and cluster health probes, with `shepherd-arch/k8stimeout` enforcement
   - Marked the KubeVirt provider unit-test lane complete using the current fake/mock client interface tests instead of the obsolete testcontainers note
   - Verified by `go test -count=1 ./internal/provider`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
97. ~~**P2** — Clarify Phase 4 rich VM revision service scope~~ ✅ **Done** (2026-06-19)
   - Marked RevisionService version auto-increment, diff calculation, and compressed YAML storage as RFC-backed future scope, matching `DEFERRED_FOLLOWUPS.md`
   - Reconciled Phase 4 summary rows so rich revision diff/compression is not presented as unqualified current V1 checklist debt
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
98. ~~**P2** — Clarify Phase 2 ResourceWatcher checklist scope~~ ✅ **Done** (2026-06-19)
   - Marked ResourceWatcher list-watch, 410 Gone, reconnect, and circuit-breaker checklist rows as RFC-backed deferred V2 acceleration, matching RFC-0020 and ADR-0038's polling baseline
   - Added design-governance assertions so ResourceWatcher details cannot reappear as unqualified current Phase 2 work while V1 keeps `vm_status_sync` as the authoritative path
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
99. ~~**P2** — Close Phase 2 General and Atlas checklist drift~~ ✅ **Done** (2026-06-19)
   - Reconciled Phase 2 General with current evidence: V1 River queue `MaxWorkers` is the shipped concurrency baseline, per-cluster queue-wait remains RFC-0015, CacheService-based rebuilding UX remains deferred, and i18n checks pass
   - Marked the Phase 2 Atlas migration-script row complete based on `migrations/atlas/atlas.hcl`, checked-in Atlas SQL, and startup migration tests
   - Verified by `go run docs/design/ci/scripts/check_frontend_no_non_english_literals.go`, `bash docs/design/ci/scripts/check_no_chinese_chars.sh`, `go test -count=1 ./internal/infrastructure`, `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, `make project-completion-readiness`, and `git diff --check`
100. ~~**P2** — Speed up public hygiene scan for large local diffs~~ ✅ **Done** (2026-06-19)
   - Replaced per-line `grep`/`tr` subprocesses in `scripts/check_public_hygiene.sh` with Bash native regex matching and lowercase expansion while preserving email-domain and private-path leak detection
   - Added a `--self-test` mode and wired `make public-hygiene-scan` to run it before scanning repository diffs
   - Verified by `bash scripts/check_public_hygiene.sh --self-test`, `make public-hygiene-scan`, and `make ci-governance` (`elapsed=0:31.27` on the current large-diff worktree)
101. ~~**P2** — Remove workflow/make parity wrapper indirection~~ ✅ **Done** (2026-06-19)
   - Replaced the remaining `ci-governance` shell-wrapper call with a direct `go run docs/design/ci/scripts/check_workflow_make_parity.go` invocation
   - Removed the obsolete `check_workflow_make_target_parity.sh` compatibility wrapper after all external references were updated
   - Verified by `go run docs/design/ci/scripts/check_workflow_make_parity.go`, `make ci-parity`, and `make ci-governance`
102. ~~**P2** — Expose live E2E status polling as a Make target~~ ✅ **Done** (2026-06-19)
   - Added `make live-e2e-status` with `LIVE_E2E_STATE_FILE` override support so operators can poll background live E2E status without streaming logs
   - Documented the Make target in the live E2E SOP and CI guide, and added design-governance assertions so the target and SOP reference stay in sync
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, fixture-backed `make live-e2e-status`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`
103. ~~**P2** — Re-verify workflow-equivalent PR CI after governance/live-E2E target changes~~ ✅ **Done** (2026-06-19)
   - Ran the top-level `make pr-ci` bundle after removing the workflow/make parity wrapper and adding `make live-e2e-status`
   - Covered preflight API sync and Ent codegen plus backend, governance, frontend, and mock e2e smoke lanes through the real parallel PR orchestration script
   - Verified with `make pr-ci` (`backend` 1m47s, `governance` 0m48s, `frontend` 7m32s, `e2e-smoke` 1m57s; overall PASS)
104. ~~**P2** — Reduce OpenAPI lint warnings for observability/adoption contract metadata~~ ✅ **Done** (2026-06-19)
   - Added the missing global `observability` tag and descriptions for pending-adoption enums plus admin observability signal schemas
   - Regenerated the OpenAPI compat artifact, runtime embedded spec, Go server types, and frontend TypeScript API types from the canonical contract
   - Verified by `make api-compat-generate`, `make api-generate`, `make ci-api-lint` (warnings reduced 18 -> 8; quality score 91 -> 94), and `make ci-api-sync-local`
105. ~~**P2** — Clear remaining OpenAPI lint warnings for adoption/observability examples~~ ✅ **Done** (2026-06-19)
   - Added request-body descriptions for pending-adoption reject/adopt actions and examples for adoption list plus admin observability summary responses
   - Added array property examples for pending adoption, trace summary, and audit signal summary schemas so Vacuum no longer reports missing-example warnings
   - Verified by `make api-compat-generate`, `make api-generate`, `make ci-api-lint` (0 warnings; A+ score), `make ci-api-sync-local`, `make ci-go-build`, and OpenAPI critical/frontend usage/doc-claim governance checks
106. ~~**P2** — Clear OpenAPI duplicate-description lint informs~~ ✅ **Done** (2026-06-19)
   - Replaced repeated response, parameter, and schema property descriptions with endpoint/schema-specific text while preserving paths, operations, enums, required fields, and schemas
   - Regenerated the OpenAPI compat artifact, runtime embedded spec, Go server types, and frontend TypeScript API types
   - Verified by `make ci-api-lint` (100/100, no warnings or informs), `make ci-api-sync-local`, and generated-code sync
107. ~~**P2** — Make live E2E status polling safe before the first background run~~ ✅ **Done** (2026-06-19)
   - Updated `scripts/run_e2e_live.sh --status` so a missing default state file reports `running=no` with `state=missing` instead of failing as an argument error
   - Documented the no-state status behavior in the live E2E validation SOP
   - Verified by `make live-e2e-status`, `bash -n scripts/run_e2e_live.sh`, and fixture-backed `bash scripts/run_e2e_live.sh --status --state-file <tmp>/latest.env`
108. ~~**P2** — Bound KubeVirt schema maintenance HTTP requests~~ ✅ **Done** (2026-06-19)
   - Added 30s request contexts to the GitHub release freshness check and KubeVirt swagger download path so maintenance commands cannot hang indefinitely on upstream HTTP calls
   - Added custom-transport unit coverage proving both maintenance HTTP helpers send requests with deadlines while preserving GitHub token/header behavior
   - Verified by `go test -count=1 ./cmd/kubevirt-schema-check ./cmd/kubevirt-schema-upgrade`
109. ~~**P2** — Make worker-pool queued submit cancellation prompt and deterministic~~ ✅ **Done** (2026-06-19)
   - Updated `Pool.Submit` so saturated-pool submission waits no longer pin the caller after the caller context is cancelled; queued wrappers still re-check context before running
   - Replaced the fixed-sleep cancellation test with an ants `Waiting()`-based synchronization point that proves `Submit` returns `context.Canceled` before the blocking worker is released
   - Verified by `go test -count=1 ./internal/pkg/worker` and `go test -race -count=1 ./internal/pkg/worker`
110. ~~**P2** — Bound application shutdown contexts for River/modules/tracing~~ ✅ **Done** (2026-06-19)
   - Added `Application.ShutdownContext(ctx)` and made the existing `Shutdown()` use a default 30s timeout before stopping River, modules, worker pools, database clients, and tracing
   - Added lifecycle tests proving default shutdown passes a bounded context and explicit shutdown preserves the caller-provided deadline
   - Verified by `go test -count=1 ./internal/app`
111. ~~**P2** — Bound the operational seed command with an explicit timeout~~ ✅ **Done** (2026-06-19)
   - Added a default 2 minute context timeout to `cmd/seed` so database initialization, built-in role reconciliation, and default-admin bootstrap cannot wait indefinitely
   - Added `SEED_TIMEOUT` as a documented Go-duration override for slow deployment databases, with validation for invalid and non-positive values
   - Verified by `go test -count=1 ./cmd/seed`
112. ~~**P2** — Bound the live E2E fixture seed command with an explicit timeout~~ ✅ **Done** (2026-06-19)
   - Added a default 2 minute context timeout to `cmd/e2e-seed` so low-level live E2E fixture reconciliation cannot wait indefinitely on database operations
   - Added `E2E_SEED_TIMEOUT` as a documented positive Go-duration override for slow live E2E databases
   - Verified by `go test -count=1 ./cmd/e2e-seed`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
113. ~~**P2** — Make Postgres login rate-limit schema initialization retryable and request-bounded~~ ✅ **Done** (2026-06-19)
   - Replaced `sync.Once` caching with explicit successful-initialization tracking so a canceled or transiently failed first login attempt does not permanently poison the Postgres rate-limit store
   - Passed the caller context into schema initialization while retaining the 5 second per-attempt upper bound
   - Verified by `go test -count=1 ./internal/api/handlers -run 'Test(PostgresLoginAttemptStore|LoginAttemptLimiter)'`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
114. ~~**P2** — Bound server construction startup reads~~ ✅ **Done** (2026-06-19)
   - Added a default 5 second initialization context for `NewServer` startup reads that load the active external approval provider and external-auth platform setting
   - Preserved existing fallback behavior when startup reads fail while preventing those Ent queries from waiting indefinitely
   - Verified by `go test -count=1 ./internal/api/handlers -run 'TestNewServer|TestServerInitializationContext'`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
115. ~~**P2** — Make auth-session and VNC replay schema initialization retryable~~ ✅ **Done** (2026-06-19)
   - Replaced `sync.Once` failure caching in `AuthSessionManager` and `PostgresVNCReplayStore` with successful-initialization tracking so canceled or transient first attempts do not poison JWT/session validation or single-use VNC replay protection
   - Passed caller contexts into both schema initializers while retaining the existing 5 second per-attempt upper bound
   - Verified by `go test -count=1 ./internal/service -run 'Test(AuthSessionManager|PostgresVNCReplayStore|VNCTokenManager)'`, `go test -count=1 ./internal/service`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
116. ~~**P2** — Make KubeVirt CR capability probing retry after transient GET failures~~ ✅ **Done** (2026-06-19)
   - Replaced exact-once KubeVirt CR loading in the kubecli adapter with successful-load caching so a transient failed `get kubevirts/kubevirt` does not poison `GetVersion`/`GetFeatureGates` for the rest of the probe instance
   - Preserved the successful-load cache so health/capability detection still shares one CR GET across version and explicit feature-gate reads
   - Verified by `go test -count=1 ./internal/provider -run 'TestKubeVirtKVCRClient|TestKubeconfigClusterFactory|TestStartKubeClientSpan'`, `go test -count=1 ./internal/provider`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
117. ~~**P2** — Bound Prometheus custom collector provider contexts~~ ✅ **Done** (2026-06-19)
   - Added a default 2 second collector scrape context for PostgreSQL table stats, River queue stats, and business metrics collectors so custom providers and zero-timeout configurations receive a bounded context
   - Preserved the existing provider-level timeout behavior so PGX providers can still apply their configured scrape timeout inside the collector deadline
   - Verified by `go test -count=1 ./internal/observability -run 'Test(PostgresTableStatsCollector|RiverQueueStatsCollector|BusinessMetricsCollector|NormalizeBusinessApproval)'`, `go test -count=1 ./internal/observability`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
118. ~~**P2** — Propagate request cancellation into VM console WebSocket proxy~~ ✅ **Done** (2026-06-19)
   - Connected the VM console proxy lifecycle to the request context so client disconnects, request cancellation, and server shutdown actively close both the WebSocket and KubeVirt backend stream instead of leaving read/write pumps blocked
   - Centralized expected console close classification so normal context cancellation, deadlines, EOFs, closed pipes, closed network connections, and standard WebSocket closes do not emit warning logs
   - Verified by `go test -count=1 ./internal/api/handlers -run 'TestProxyConsoleWebSocketStopsWhenParentContextCancels|TestConsoleOriginAllowed|TestVMConsole_OpenVNC'`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
119. ~~**P2** — Bound Tempo trace query response bodies~~ ✅ **Done** (2026-06-19)
   - Added a default 8 MiB per-response cap to the Tempo trace query client so administrator observability requests cannot decode unbounded search or trace-detail JSON responses from an external Tempo endpoint
   - Kept the existing request context timeout behavior and added an internal `MaxResponseBytes` option to make oversized-response rejection deterministic in tests
   - Verified by `go test -count=1 ./internal/observability -run 'TestTempoTraceQueryClient|TestTraceAccumulator|TestTempoSearchTags'`, `go test -count=1 ./internal/observability`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
120. ~~**P2** — Bound WeCom auth provider response bodies and preserve response-read context~~ ✅ **Done** (2026-06-19)
   - Added a default 1 MiB response cap to the built-in WeCom auth provider so gettoken, userinfo, user profile, and department-list responses cannot decode unbounded external JSON during login and admin connection tests
   - Fixed the WeCom request timeout lifecycle so the per-request timeout context is cancelled after `resp.Body` is closed, not before the caller reads the body
   - Verified by `go test -count=1 ./internal/provider -run 'TestWeCom'`, `go test -count=1 ./internal/provider`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
121. ~~**P2** — Bound KubeVirt schema maintenance HTTP response bodies~~ ✅ **Done** (2026-06-19)
   - Added a default 2 MiB cap to the KubeVirt release freshness checker so GitHub releases JSON cannot be decoded from an unbounded response body
   - Added a default 64 MiB cap to the KubeVirt schema upgrade swagger download path while preserving the existing 30 second request timeout and token/header behavior
   - Verified by `go test -count=1 ./cmd/kubevirt-schema-check ./cmd/kubevirt-schema-upgrade`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
122. ~~**P2** — Remove blocked submit goroutine leakage from worker pool cancellation~~ ✅ **Done** (2026-06-19)
   - Switched the underlying ants pools to non-blocking submission and moved pool-full waiting into Shepherd's context-aware `Pool.Submit` loop so caller cancellation no longer leaves a background goroutine blocked inside `ants.Pool.Submit`
   - Applied the same context-aware submission path to detached tasks so service shutdown cancels queued detached submissions before they enter the pool
   - Verified by `go test -count=1 ./internal/pkg/worker`, `go test -race -count=1 ./internal/pkg/worker`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
123. ~~**P2** — Preserve VM job lifecycle state on worker context cancellation~~ ✅ **Done** (2026-06-19)
   - Added a shared VM job cancellation guard so worker `context.Canceled`/expired worker contexts return to River before cluster-runtime snooze or business-failure classification
   - Applied the guard to VM create, modify, delete, and power K8s/provider error paths so process shutdown or active job cancellation does not persist false `FAILED` DomainEvent/Ticket/VM state or stale reject reasons
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
124. ~~**P2** — Preserve VM status-sync poll chain semantics on worker context cancellation~~ ✅ **Done** (2026-06-19)
   - Made `VMStatusSyncWorker` return worker context cancellation before resource-version recovery, transient K8s rescheduling, or self-schedule graceful-degradation paths
   - Guarded `scheduleNext` itself so a cancelled River worker context cannot be downgraded to a successful no-op when the River client provider is absent
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
125. ~~**P2** — Preserve directory sync job state on worker context cancellation~~ ✅ **Done** (2026-06-19)
   - Made `DirectorySyncWorker` return worker context cancellation before persisting provider-load, directory-list, or per-record apply failures as directory sync business failures
   - Guarded `failJob` itself so a cancelled River worker context cannot be downgraded to a successful persisted `FAILED` directory sync job
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
126. ~~**P2** — Preserve scheduled directory enrichment scan semantics on worker context cancellation~~ ✅ **Done** (2026-06-19)
   - Made the scheduled enrichment scan loop stop and return worker context cancellation instead of logging the provider evaluation error and completing the River scan job successfully
   - Guarded pre-create scheduled enrichment paths so a cancelled River worker context does not create a failed `DirectorySyncJob`; post-create enqueue cancellation cleanup is covered by item 128
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
127. ~~**P2** — Preserve VM tombstone cleanup semantics on worker context cancellation~~ ✅ **Done** (2026-06-19)
   - Made VM tombstone cleanup return worker context cancellation from candidate listing, active-delete-event checks, and per-row hard-delete attempts instead of treating cancellation as a successful cleanup run
   - Preserved the existing behavior for ordinary per-row hard-delete failures so one blocked tombstone still cannot starve later cleanup candidates
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
128. ~~**P2** — Prevent scheduled directory enrichment enqueue cancellation from orphaning pending jobs~~ ✅ **Done** (2026-06-19)
   - Original failed-row cleanup semantics were superseded by item 155: scheduled `DirectorySyncJob` creation and River enqueue now share one transaction, so enqueue failure rolls back the job row instead of persisting a failed cleanup marker
   - Cancellation before durable enqueue still returns `context.Canceled` without creating a row, and due checks no longer see a permanent `pending` blocker without a River job
   - Verified by item 155's scheduled enqueue rollback coverage plus `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
129. ~~**P2** — Prevent manual directory sync enqueue cancellation from orphaning pending jobs~~ ✅ **Done** (2026-06-19)
   - Original failed-row cleanup semantics were superseded by item 155: manual `DirectorySyncJob` creation and River enqueue now share one transaction, so enqueue failure rolls back the job row instead of exposing a failed cleanup marker to users
   - This preserves the original goal of avoiding permanent `pending` jobs without River work, while making job visibility match actual enqueue success
   - Verified by item 155's manual enqueue success/failure coverage plus `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
130. ~~**P2** — Preserve VM status-sync state on DB persistence cancellation~~ ✅ **Done** (2026-06-19)
   - `VMStatusSyncWorker` now returns worker context cancellation directly when status persistence, missing-VM reconciliation, or stale resourceVersion reset DB writes are interrupted
   - Added regression coverage proving those interrupted persistence paths leave the VM row unchanged and return `context.Canceled` instead of a wrapped ordinary worker error
   - Verified by `go test -count=1 -run 'TestVMStatusSyncWorkerWork_.*CancellationReturnsContextError|TestVMStatusSyncWorkerWork_ClearsCachedResourceVersionOnExpiredList|TestVMStatusSyncWorkerWork_PersistsObservedStatusAndResourceVersion|TestVMStatusSyncWorkerWork_MarksMissingVMNotFound' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
131. ~~**P2** — Preserve directory sync job state on DB persistence cancellation~~ ✅ **Done** (2026-06-19)
   - `DirectorySyncWorker` now returns worker context cancellation directly when loading the job row, marking a job `running`, completing a job, or persisting a business failure is interrupted
   - Added regression coverage proving interrupted `running`, `completed`, and `failed` persistence paths leave the job in the last durable state and return `context.Canceled`
   - Verified by `go test -count=1 -run 'TestDirectorySyncWorker_' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
132. ~~**P2** — Preserve VM create final state on DB persistence cancellation~~ ✅ **Done** (2026-06-19)
   - `VMCreateWorker` now returns worker context cancellation directly when the final VM status update or DomainEvent `COMPLETED` write is interrupted after the K8s create/idempotency success path
   - Added regression coverage for both interruption points, proving event/ticket rows remain in the last durable state and VM status is either unchanged or already advanced before the interrupted write
   - Verified by `go test -count=1 -run 'TestVMCreateWorker_(ContextCancellationDoesNotFailEventTicketOrVM|VMStatusPersistCancellationReturnsContextError|EventCompletePersistCancellationReturnsContextError)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/vm_create.go internal/jobs/vm_create_worker_test.go docs/design/CHECKLIST.md`
133. ~~**P2** — Preserve VM modify final state on DB persistence failure~~ ✅ **Done** (2026-06-19)
   - `VMModifyWorker` now returns worker context cancellation directly when post-mutation VM status persistence is interrupted, and returns retryable errors when final VM status or DomainEvent `COMPLETED` persistence fails after a successful live mutation
   - Added regression coverage proving interrupted status persistence leaves event/ticket/VM rows in the last durable state, and ordinary `COMPLETED` persistence failures no longer let the River job finish successfully with the event stuck in `PROCESSING`
   - Verified by `go test -count=1 -run 'TestVMModifyWorker_(ExecutesLiveMemoryUpdateAndPersistsStatus|ContextCancellationDoesNotFailTicket|StatusPersistCancellationReturnsContextError|EventCompletePersistFailureReturnsRetryableError)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/vm_modify.go internal/jobs/vm_modify_test.go docs/design/CHECKLIST.md`
134. ~~**P2** — Preserve VM delete final event state on DB persistence failure~~ ✅ **Done** (2026-06-19)
   - `VMDeleteWorker` now returns worker context cancellation directly when the final DomainEvent `COMPLETED` write is interrupted after delete cleanup, and returns a retryable error for ordinary final event persistence failures instead of silently completing the River job
   - Added regression coverage proving K8s/DB delete side effects remain durable while event/ticket rows stay in the last durable state when final event persistence fails or is cancelled
   - Verified by `go test -count=1 -run 'TestVMDeleteWorker_(AllowsDeletingStatusAtExecutionTime|EventCompletePersistFailureReturnsRetryableError|EventCompletePersistCancellationReturnsContextError|ContextCancellationDoesNotFailEventTicketOrVM)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/vm_delete.go internal/jobs/vm_delete_test.go docs/design/CHECKLIST.md`
135. ~~**P2** — Preserve VM power final persistence cancellation semantics~~ ✅ **Done** (2026-06-19)
   - `VMPowerWorker` now returns worker context cancellation directly when post-power VM status persistence or final DomainEvent `COMPLETED` persistence is interrupted after the lifecycle operation succeeds
   - Kept ordinary post-power DB persistence failures on the existing log-only path to avoid unsafe automatic retries of non-idempotent operations such as restart, while preventing cancelled workers from being reported as successful
   - Added regression coverage proving the external stop can have taken effect while event/ticket/VM rows remain in the last durable state on status-persistence cancellation, and that event-persistence cancellation preserves the already-updated VM row without marking success
   - Verified by `go test -count=1 -run 'TestVMPowerWorker_(ContextCancellationDoesNotFailEventOrTicket|StatusPersistCancellationReturnsContextError|EventCompletePersistCancellationReturnsContextError|UsesObservedLiveStatusAfterStop)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/vm_power.go internal/jobs/vm_power_test.go docs/design/CHECKLIST.md`
136. ~~**P2** — Make completed VM events repair durable ticket success state~~ ✅ **Done** (2026-06-19)
   - Added a propagated ticket-status helper for success paths while preserving the existing best-effort helper for failure paths, so VM create/modify/delete/power final `SUCCESS` ticket persistence failures now return retryable worker errors
   - Completed-event idempotency paths now repair stale `EXECUTING` tickets to `SUCCESS` before skipping external work, making retries after a completed DomainEvent safe and self-healing for ticket state
   - Added regression coverage for create final ticket persistence failure and completed-event ticket repair across create/modify/delete/power workers
   - Verified by `go test -count=1 -run 'TestVM(CreateWorker_(CompletedEventRepairsTicketSuccessWithoutCreating|TicketSuccessPersistFailureReturnsRetryableError)|ModifyWorker_SkipsCompletedEventWithoutExecutingProvider|DeleteWorker_SkipsCompletedEventWithoutExecutingProvider|PowerWorker_SkipsCompletedEventWithoutExecutingProvider)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/helpers.go internal/jobs/vm_create.go internal/jobs/vm_modify.go internal/jobs/vm_delete.go internal/jobs/vm_power.go internal/jobs/vm_create_worker_test.go internal/jobs/vm_modify_test.go internal/jobs/vm_delete_test.go internal/jobs/vm_power_test.go docs/design/CHECKLIST.md`
137. ~~**P2** — Propagate parent batch aggregate sync failures from durable VM success paths~~ ✅ **Done** (2026-06-19)
   - Split parent batch status recalculation into returning internal helpers and best-effort public wrappers, so success-path ticket updates can fail the River worker when parent batch ticket/event/projection state cannot be synchronized
   - `updateTicketStatusByEvent` now propagates parent batch sync failures after the child ticket update succeeds, letting completed-event retry paths repair stale parent batch aggregates without repeating external VM work
   - Added regression coverage proving parent batch event persistence failure is returned while already-durable child ticket, parent ticket, and projection updates remain available for retry repair
   - Verified by `go test -count=1 -run 'Test(UpdateTicketStatusByEventPropagatesParentBatchSyncFailure|SyncParentBatchStatus_RecalculatesBatchAggregates)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/helpers.go internal/jobs/helpers_test.go docs/design/CHECKLIST.md`
138. ~~**P2** — Persist VM permanent-failure state before cancelling malformed jobs~~ ✅ **Done** (2026-06-19)
   - Added a durable `FAILED` Event/Ticket helper and used it before `JobCancel` in VM create/modify/delete/power malformed-payload paths plus the unknown power operation path
   - If the permanent-failure state cannot be persisted, workers now return retryable errors or worker context cancellation instead of cancelling the River job and leaving the request visible as pending/processing
   - Added regression coverage proving failed-state persistence errors are propagated and malformed create payloads no longer return `JobCancel` before durable failure state is saved
   - Verified by `go test -count=1 -run 'Test(PersistFailedEventAndTicketByEventPropagatesEventFailure|VMCreateWorker_MalformedPayloadFailurePersistErrorIsRetryable)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/helpers.go internal/jobs/helpers_test.go internal/jobs/vm_create.go internal/jobs/vm_modify.go internal/jobs/vm_delete.go internal/jobs/vm_power.go internal/jobs/vm_create_worker_test.go docs/design/CHECKLIST.md`
139. ~~**P2** — Persist VM permanent-failure state before cancelling validation rejections~~ ✅ **Done** (2026-06-19)
   - VM create/modify/delete/power permanent validation and runtime rejection paths now persist `FAILED` Event/Ticket state before returning `JobCancel`
   - If the `FAILED` state cannot be persisted, workers return worker context cancellation or retryable errors instead of cancelling River jobs while user-visible rows remain pending or processing
   - Added regression coverage proving VM create permanent validation persistence failures remain retryable and do not return `JobCancel`
   - Verified by `go test -count=1 -run 'TestVMCreateWorker_(MalformedPayloadFailurePersistErrorIsRetryable|PermanentValidationFailurePersistErrorIsRetryable)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check -- internal/jobs/vm_create.go internal/jobs/vm_modify.go internal/jobs/vm_delete.go internal/jobs/vm_power.go internal/jobs/vm_create_worker_test.go docs/design/CHECKLIST.md`; real live E2E is still not complete: `make live-e2e-status` reports `state=missing`, and `make ci-live-e2e-latest-evidence` still fails because no full manifest exists under `.run/live-e2e`
140. ~~**P2** — Keep retryable VM lifecycle provider failures non-terminal~~ ✅ **Done** (2026-06-19)
   - VM create/delete provider failures now distinguish retry attempts from final attempts: non-final ordinary River errors keep Event/Ticket/VM rows non-terminal, while final attempts persist `FAILED` Event/Ticket/VM state before letting River finalize the error
   - Added a shared final-attempt helper based on River `JobRow.Attempt`/`MaxAttempts`, and narrowed the remaining best-effort ticket helper to marking lifecycle workers `EXECUTING`
   - Added regression coverage proving ordinary create/delete provider errors stay retryable on attempt 1/3 and persist terminal `FAILED` state on attempt 3/3
   - Verified by `go test -count=1 -run 'TestVM(CreateWorker_(RetryableCreateFailureDoesNotPersistTerminalFailure|FinalCreateFailurePersistsTerminalFailure)|DeleteWorker_(RetryableDeleteFailureDoesNotPersistTerminalFailure|FinalDeleteFailurePersistsTerminalFailure))' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
141. ~~**P2** — Keep retryable VM modify/power provider failures non-terminal~~ ✅ **Done** (2026-06-19)
   - VM modify/power provider failures now match the create/delete River retry contract: non-final ordinary errors leave Event/Ticket rows non-terminal, while final attempts persist `FAILED` request state before River finalizes the error
   - Permanent modify/power cancellation paths still persist `FAILED` before returning `JobCancel`; modify final failures also preserve the ticket reject reason without marking the VM runtime row failed
   - Added regression coverage proving modify and power provider errors stay retryable on attempt 1/3 and persist terminal request `FAILED` state on attempt 3/3
   - Verified by `go test -count=1 -run 'TestVM(ModifyWorker_(RetryableMutationFailureDoesNotPersistTerminalFailure|FinalMutationFailurePersistsTerminalFailure)|PowerWorker_(RetryablePowerFailureDoesNotPersistTerminalFailure|FinalPowerFailurePersistsTerminalFailure))' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
142. ~~**P2** — Retry idempotent VM power persistence failures after successful start/stop~~ ✅ **Done** (2026-06-19)
   - `VMPowerWorker` now returns retryable errors when `start`/`stop` succeeds externally but local VM status or DomainEvent `COMPLETED` persistence fails, letting River retry and repair durable state through the existing idempotent conflict handling
   - `restart` keeps the existing log-only persistence-failure behavior because retrying after an externally successful restart can repeat a non-idempotent operation
   - Added regression coverage proving successful `stop` with VM status or event completion persistence failure returns a retryable error and leaves ticket/event state non-terminal for retry repair
   - Verified by `go test -count=1 -run 'TestVMPowerWorker_Stop(StatusPersistFailureReturnsRetryableError|EventCompletePersistFailureReturnsRetryableError)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
143. ~~**P2** — Mark VM power events PROCESSING before external execution~~ ✅ **Done** (2026-06-19)
   - `VMPowerWorker` now persists DomainEvent `PROCESSING` before parsing/executing non-completed power events, aligning direct and batch power jobs with the visible lifecycle already used by create/modify/delete approvals
   - If the `PROCESSING` write fails, the worker returns a retryable error before calling the provider, preventing external side effects while the durable request row still looks pending
   - Added regression coverage proving processing-persist failures do not call the provider, and transient/retry/cancellation/persistence-failure paths now leave in-flight power events as `PROCESSING` instead of stale `PENDING`
   - Verified by `go test -count=1 -run 'TestVMPowerWorker_(EventProcessingPersistFailureDoesNotExecuteProvider|SnoozesTransientClusterErrorsWithoutFailingTicket|RetryablePowerFailureDoesNotPersistTerminalFailure|ContextCancellationDoesNotFailEventOrTicket|StopStatusPersistFailureReturnsRetryableError|StopEventCompletePersistFailureReturnsRetryableError|StatusPersistCancellationReturnsContextError|EventCompletePersistCancellationReturnsContextError)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`
144. ~~**P2** — Skip stale VM lifecycle jobs for all terminal DomainEvents~~ ✅ **Done** (2026-06-19)
   - VM create/modify/delete/power workers now treat `COMPLETED`, `FAILED`, and `CANCELLED` DomainEvents as terminal before payload parsing or provider execution, repairing the associated Ticket to `SUCCESS`, `FAILED`, or `CANCELLED` and returning without external side effects
   - Stale River jobs for failed or cancelled power events can no longer overwrite the event back to `PROCESSING` before calling KubeVirt
   - Added regression coverage proving terminal create/modify/delete/power events skip provider calls, keep the DomainEvent terminal, and repair Ticket status through the shared terminal-status mapping
   - Verified by `go test -count=1 -run 'Test(TicketStatusForTerminalDomainEvent|VMCreateWorker_SkipsFailedEventWithoutCreating|VMModifyWorker_SkipsFailedEventWithoutExecutingProvider|VMDeleteWorker_SkipsFailedEventWithoutExecutingProvider|VMPowerWorker_SkipsTerminalEventWithoutExecutingProvider)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
145. ~~**P2** — Make single-ticket terminal decision updates atomic~~ ✅ **Done** (2026-06-19)
   - VNC approval, single-ticket rejection, and requester cancellation now update Ticket and DomainEvent state in one Ent transaction before audit/notification side effects
   - If the DomainEvent terminal-state write fails, the Ticket update is rolled back instead of leaving `APPROVED`/`REJECTED`/`CANCELLED` tickets attached to still-pending events
   - Added hook-injected regression coverage for VNC approve, reject, and cancel DomainEvent write failures, proving the Ticket remains `PENDING` after rollback
   - Verified by `go test -count=1 -run 'TestService(ApproveVNC_(TransitionsTicketAndEventWithoutAtomicWriter|RollsBackTicketWhenEventCompletionFails)|Reject_(TransitionsTicketAndEvent|RollsBackTicketWhenEventCancellationFails)|Cancel_(RequesterTransitionsTicketAndEvent|RollsBackTicketWhenEventCancellationFails))' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
146. ~~**P2** — Make batch parent terminal decisions atomic across children~~ ✅ **Done** (2026-06-19)
   - Batch parent rejection and requester cancellation now update the parent Ticket/Event plus all pending child Ticket/Event rows in one Ent transaction before audit/notification/projection side effects
   - If the child DomainEvent bulk cancellation write fails, parent and child Tickets roll back to `PENDING` instead of leaving terminal tickets attached to still-pending events
   - Added hook-injected regression coverage for batch reject and batch cancel child-event cancellation failures, proving parent/child Ticket and Event rows remain pending after rollback
   - Verified by `go test -count=1 -run 'TestService(RejectBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|CancelBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|ApproveVNC_RollsBackTicketWhenEventCompletionFails|Reject_RollsBackTicketWhenEventCancellationFails|Cancel_RollsBackTicketWhenEventCancellationFails)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
147. ~~**P2** — Make batch approval parent summary updates atomic~~ ✅ **Done** (2026-06-19)
   - Batch parent approval now writes the final parent Ticket summary and parent DomainEvent summary in one Ent transaction after child dispatch completes
   - If the parent DomainEvent summary write fails, the parent Ticket status/approver/placement fields roll back instead of leaving an executing/failed parent ticket attached to a stale parent event
   - Added hook-injected regression coverage for parent event summary persistence failure after batch delete child dispatch, proving the parent summary rollback leaves parent rows pending
   - Verified by `go test -count=1 -run 'TestService(ApproveBatchParent_RollsBackParentSummaryWhenParentEventUpdateFails|RejectBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|CancelBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
148. ~~**P2** — Make batch child dispatch-failure persistence atomic and blocking~~ ✅ **Done** (2026-06-19)
   - Batch approval now marks a failed child Ticket and child DomainEvent in one Ent transaction, and propagates persistence failures instead of continuing to parent summary writes
   - If the child failure Event write fails, the child Ticket rollback keeps the child pending and the parent remains pending rather than reporting a summary based on unpersisted child failure state
   - Added hook-injected regression coverage for batch delete child dispatch failure plus child failure-event persistence failure, proving parent and child rows remain pending after rollback
   - Verified by `go test -count=1 -run 'TestService(ApproveBatchParent_(StopsWhenChildFailurePersistenceFails|RollsBackParentSummaryWhenParentEventUpdateFails)|RejectBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|CancelBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
149. ~~**P2** — Make worker parent-batch status sync atomic~~ ✅ **Done** (2026-06-19)
   - The shared jobs parent-batch sync helper now updates parent Ticket status, parent DomainEvent status, and BatchTicket projection counters in one Ent transaction
   - If the parent event or projection update fails, parent Ticket/Event/projection summary writes roll back instead of leaving partially synchronized parent batch state
   - Updated existing parent-event failure coverage and added projection-failure regression coverage proving parent summary rows stay at their previous in-progress state while the child status update remains persisted
   - Verified by `go test -count=1 -run 'Test(UpdateTicketStatusByEvent(PropagatesParentBatchSyncFailure|RollsBackParentBatchSyncOnProjectionFailure)|SyncParentBatchStatus_RecalculatesBatchAggregates)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
150. ~~**P2** — Make VM failure Event/Ticket persistence atomic~~ ✅ **Done** (2026-06-19)
   - The shared VM failure helper now persists DomainEvent `FAILED` and the associated Ticket `FAILED` in one Ent transaction before attempting parent-batch sync repair
   - If the Ticket update fails, the DomainEvent update rolls back instead of leaving a failed event attached to an executing or pending ticket
   - Added hook-injected regression coverage for Ticket persist failure, alongside the existing Event persist failure coverage, proving both failure directions avoid half-written failed request state
   - Verified by `go test -count=1 -run 'TestPersistFailedEventAndTicketByEvent(PropagatesEventFailure|RollsBackEventOnTicketFailure)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
151. ~~**P2** — Persist final VM modify failure reason atomically~~ ✅ **Done** (2026-06-19)
   - Final VM modify failures now persist DomainEvent `FAILED`, Ticket `FAILED`, and Ticket `reject_reason` through the same Ent transaction instead of writing the reason in a second step after terminal state
   - If the Ticket update or reject-reason write fails, the DomainEvent update rolls back so retries do not skip a terminal event whose failure reason was never recorded
   - Added direct regression coverage for successful final modify failure persistence and hook-injected Ticket persistence failure rollback
   - Verified by `go test -count=1 -run 'Test(PersistFinalModifyFailure_(PersistsRejectReasonAtomically|RollsBackTerminalStateOnRejectReasonPersistFailure)|VMModifyWorker_FinalMutationFailurePersistsTerminalFailure)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
152. ~~**P2** — Persist final VM create/delete failure state atomically~~ ✅ **Done** (2026-06-19)
   - Final VM create/delete provider failures now persist VM `FAILED`, DomainEvent `FAILED`, and Ticket `FAILED` through one shared Ent transaction instead of writing the VM row before terminal request state
   - If the Event or Ticket terminal write fails, the final VM `FAILED` write rolls back so retries do not observe a request that is still active while the VM row already looks terminal
   - Updated the Stage 5.C status-progression governance check to recognize the shared transactional helper while preserving the `CREATING -> RUNNING|FAILED` rule
   - Verified by `go test -count=1 -run 'TestVM(CreateWorker_FinalCreateFailure(PersistsTerminalFailure|RollsBackVMOnEventPersistFailure)|DeleteWorker_FinalDeleteFailure(PersistsTerminalFailure|RollsBackVMOnEventPersistFailure))' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `go run docs/design/ci/scripts/check_vm_create_status_progression.go`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
153. ~~**P2** — Make admin user deletion cleanup atomic~~ ✅ **Done** (2026-06-19)
   - Admin user deletion now removes external cohort grants, global role bindings, resource role bindings, user preferences, and the user row inside one Ent transaction
   - If any associated-row cleanup fails, earlier deletes roll back instead of leaving a partially deleted identity/RBAC graph
   - Added hook-injected regression coverage that fails resource-role-binding cleanup after earlier associated deletes and proves all user-owned rows remain
   - Verified by `go test -count=1 -run 'Test(AdminUserRoleBindingAndAuthProviderCRUD|DeleteUser_RollsBackAssociatedRowsWhenDeleteStepFails|UpdateUser_RollsBackWhenSessionRevocationFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
154. ~~**P2** — Make external cohort RBAC reconciliation transaction-scoped~~ ✅ **Done** (2026-06-19)
   - Directory sync workers now apply each imported/enriched record through the jobs-layer Ent transaction boundary and pass a transaction-scoped `DirectorySyncService` client into service code
   - External cohort grant and managed `RoleBinding` writes/deletes now use the caller-provided Ent client, so login and directory-sync callers can roll back the RBAC graph as one unit without service-layer `Tx()` calls
   - Added hook-injected regression coverage for grant-create and role-binding-delete failures, proving paired grant/binding writes roll back when the caller supplies a transaction
   - Verified by `go test -count=1 -run 'TestExternalAuthService_(UpsertExternalUser_ReconcilesManagedBindings|ReconcileManagedBindingRollsBackRoleBindingOnGrantCreateFailure|ReconcileManagedBindingRollsBackGrantDeleteOnRoleBindingDeleteFailure)' -v ./internal/service`, `go test -count=1 ./internal/service`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
155. ~~**P2** — Make directory sync job creation and River enqueue atomic~~ ✅ **Done** (2026-06-19)
   - Manual directory sync requests and scheduled enrichment scans now create the `DirectorySyncJob` row and enqueue the River claim-check job through one `pgx.Tx` using River `InsertTx`
   - If River enqueue fails, the `DirectorySyncJob` insert rolls back instead of leaving a pending or failed job row that was never actually enqueued
   - Added regression coverage for successful manual sync enqueue plus manual and scheduled enqueue failure rollback, including the scheduled scanner dependency path
   - Verified by `go test -count=1 -run 'TestSyncAuthProviderDirectory_(CreatesJobAndRiverEntryAtomically|EnqueueFailureRollsBackJobCreate)|TestDirectoryEnrichmentScheduleScanWorker_(WorkEnqueuesDueScheduledEnrichment|EnqueueFailureRollsBackJobCreate|MissingPoolRollsBackJobCreate)' -v ./internal/api/handlers ./internal/jobs`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
156. ~~**P2** — Require scheduled enrichment enqueue transaction dependencies at worker start~~ ✅ **Done** (2026-06-19)
   - The scheduled directory enrichment scan worker now treats the `pgxpool.Pool` dependency as required during `Work` initialization, matching its River `InsertTx` enqueue path
   - Added initialization coverage proving a missing pool fails before provider scanning or River client resolution, while an empty store with complete dependencies still completes without resolving River
   - Reconciled older item 128/129 checklist wording so the historical failed-row cleanup notes no longer conflict with item 155's transaction rollback semantics
   - Verified by `go test -count=1 -run 'TestDirectoryEnrichmentScheduleScanWorker_(WorkInitializationAndEmptyStore|WorkEnqueuesDueScheduledEnrichment|EnqueueFailureRollsBackJobCreate|MissingPoolRollsBackJobCreate)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
157. ~~**P2** — Make batch power retry child failure markers atomic~~ ✅ **Done** (2026-06-19)
   - Batch power retry now marks child Ticket failure/reject reason and child DomainEvent failure inside one handler-layer Ent transaction for invalid payloads, unknown operations, child event load failures, and retry enqueue failures
   - If the child Event failure write fails, the child Ticket update rolls back instead of overwriting the previous retry failure reason on its own
   - Added hook-injected regression coverage for the unknown-operation failure-marker path, proving the seeded child Ticket reason remains unchanged after Event persistence failure
   - Verified by `go test -count=1 -run 'TestBatchHandler_RetryVMBatch_PowerChild(UnknownOperation|FailureMarkerRollsBackTicketOnEventPersistFailure|EnqueueFailure|AtomicallyEnqueues)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
158. ~~**P2** — Make batch child retry-reset and cancel status transitions atomic~~ ✅ **Done** (2026-06-19)
   - Non-power batch retry reset now moves child Tickets back to `PENDING`, clears reject reasons, and resets child DomainEvents to `PENDING` inside one handler-layer Ent transaction
   - Batch child cancel now moves child Tickets to `CANCELLED` and child DomainEvents to `CANCELLED` inside one transaction, preventing half-cancelled child state if the event write fails
   - Added hook-injected regression coverage for child cancel Event persistence failure, proving the child Ticket remains `PENDING` after rollback
   - Verified by `go test -count=1 -run 'TestBatchHandler_(SubmitDelete_GetAndCancel|CancelVMBatch_RollsBackChildTicketsWhenEventCancelFails|RetryVMBatch_RetriesFailedDeleteChild)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because no full live E2E evidence manifest exists under `.run/live-e2e`
159. ~~**P2** — Make non-power batch retry failure markers atomic~~ ✅ **Done** (2026-06-19)
   - Non-power batch retry now writes child Ticket failure/reject reason and child DomainEvent failure inside one handler-layer Ent transaction when re-approval fails
   - If the child Event failure write fails, the child Ticket remains in the reset `PENDING` state with no reject reason instead of being half-marked `FAILED`
   - Added hook-injected regression coverage for the re-approval failure path and reused the same transactional helper shape as power retry child failure markers
   - Verified by `go test -count=1 -run 'TestBatchHandler_RetryVMBatch_(RollsBackChildFailureMarkerWhenEventFailWriteFails|RetriesFailedDeleteChild|UsesApprovalProviderSeam)|TestBatchHandler_RetryVMBatch_PowerChildFailureMarkerRollsBackTicketOnEventPersistFailure|TestBatchHandler_CancelVMBatch_RollsBackChildTicketsWhenEventCancelFails' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, and `make ci-go-build`; real live E2E is still not complete because `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
160. ~~**P2** — Surface batch parent status sync failures to action callers~~ ✅ **Done** (2026-06-19)
   - `jobs.SyncParentBatchStatus` and `SyncParentBatchStatusByChildEvent` now return their transactional sync error to callers instead of forcing all public callers into best-effort behavior
   - Batch retry/cancel handlers fail the request when parent Ticket/Event/projection synchronization fails, avoiding a successful API response with stale parent batch state; cancel now performs the child Ticket/Event transitions and parent Ticket/Event/projection synchronization in the same transaction so a sync failure also rolls back the children
   - Added hook-injected regression coverage for cancel parent projection persistence failure, proving the handler returns `500` and leaves child, parent, event, and projection state unchanged
   - Verified by `go test -count=1 -run 'TestBatchHandler_CancelVMBatch_(ReturnsErrorWhenParentStatusSyncFails|RollsBackChildTicketsWhenEventCancelFails)|TestBatchHandler_SubmitDelete_GetAndCancel' -v ./internal/api/handlers`, `go test -count=1 -run 'TestSyncParentBatchStatus_RecalculatesBatchAggregates|TestUpdateTicketStatusByEventPropagatesParentBatchSyncFailure|TestUpdateTicketStatusByEventRollsBackParentBatchSyncOnProjectionFailure' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, and `make ci-go-build`; real live E2E is still not complete because `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
161. ~~**P2** — Make ticketing batch parent projection sync transactional~~ ✅ **Done** (2026-06-19)
   - Ticketing service batch parent approve/reject/cancel now synchronizes the `BatchTicket` projection inside the same decision transaction as parent Ticket/Event and child Ticket/Event mutations
   - Projection persistence failures now return to the caller and roll back reject/cancel parent and child decision state instead of logging a warning after a successful API-visible decision
   - Added hook-injected regression coverage for reject and cancel projection write failures, proving parent/child Tickets, parent/child Events, and the projection row remain pending after rollback
   - Verified by `go test -count=1 -run 'TestService(RejectBatchParent_RollsBackParentAndChildrenWhenProjectionSyncFails|CancelBatchParent_RollsBackParentAndChildrenWhenProjectionSyncFails|RejectBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|CancelBatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails|ApproveBatchParent_RollsBackParentSummaryWhenParentEventUpdateFails)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, and `make ci-go-build`; real live E2E is still not complete because `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
162. ~~**P2** — Make power worker success terminal Event/Ticket persistence atomic~~ ✅ **Done** (2026-06-19)
   - Added a jobs-layer helper that persists `DomainEvent=COMPLETED`, child Ticket `SUCCESS`, and parent batch status/projection refresh inside one Ent transaction
   - VM power success now uses that helper, so Event completion failure cannot be followed by a Ticket `SUCCESS` write; unsafe-to-repeat `restart` terminal persistence failure cancels the job instead of silently succeeding or repeating the restart
   - Refactored parent batch status sync internals so existing public sync helpers keep their behavior while transaction-scoped callers can reuse the same logic without nested transactions
   - Added hook-injected regression coverage for restart terminal persistence failure, proving Event stays `PROCESSING` and Ticket stays `EXECUTING` instead of being half-marked successful
   - Verified by `go test -count=1 -run 'TestVMPowerWorker_(RestartTerminalPersistFailureDoesNotMarkTicketSuccess|SkipsCompletedEventWithoutExecutingProvider|FinalPowerFailurePersistsTerminalFailure)|TestSyncParentBatchStatus_RecalculatesBatchAggregates|TestUpdateTicketStatusByEventPropagatesParentBatchSyncFailure|TestUpdateTicketStatusByEventRollsBackParentBatchSyncOnProjectionFailure' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-build`; real live E2E is still not complete because `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
163. ~~**P2** — Make VM create success terminal state persistence atomic~~ ✅ **Done** (2026-06-19)
   - VM create success now persists VM row status, `DomainEvent=COMPLETED`, child Ticket `SUCCESS`, and parent batch status/projection refresh inside one jobs-layer Ent transaction
   - If Event or Ticket terminal persistence fails after K8s create succeeds, the VM row status rolls back instead of showing `RUNNING` while the request remains active
   - Added hook-injected regression coverage for Event completion and Ticket success persistence failures, proving VM stays `CREATING`, Event stays `PROCESSING`, and Ticket stays `EXECUTING` after rollback
   - Verified by `go test -count=1 -run 'TestVMCreateWorker_(EventCompletePersistFailureRollsBackVMAndTicket|EventCompletePersistCancellationReturnsContextError|TicketSuccessPersistFailureReturnsRetryableError|SkipsK8sCreateWhenEventLabeledVMAlreadyExists|CompletedEventRepairsTicketSuccessWithoutCreating)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `go run docs/design/ci/scripts/check_vm_create_status_progression.go`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
164. ~~**P2** — Make VM modify success terminal state persistence atomic~~ ✅ **Done** (2026-06-19)
   - VM modify success now persists VM status sync metadata, `DomainEvent=COMPLETED`, child Ticket `SUCCESS`, and parent batch status/projection refresh inside one jobs-layer Ent transaction
   - If Event or Ticket terminal persistence fails after the K8s mutation succeeds, the VM row's `last_k8s_rv` / `last_polled_at` sync markers roll back instead of showing fresh live-state evidence while the request remains active
   - Added hook-injected regression coverage for Event completion and Ticket success persistence failures, proving VM sync metadata stays unset, Event stays `PROCESSING`, and Ticket stays `EXECUTING` after rollback
   - Verified by `go test -count=1 -run 'TestVMModifyWorker_(ExecutesLiveMemoryUpdateAndPersistsStatus|StatusPersistCancellationReturnsContextError|EventCompletePersistFailureReturnsRetryableError|TicketSuccessPersistFailureRollsBackTerminalState|SkipsCompletedEventWithoutExecutingProvider|SkipsFailedEventWithoutExecutingProvider)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
165. ~~**P2** — Make VM delete success terminal Event/Ticket persistence atomic~~ ✅ **Done** (2026-06-19)
   - VM delete success now persists `DomainEvent=COMPLETED`, child Ticket `SUCCESS`, and parent batch status/projection refresh inside one jobs-layer Ent transaction after the K8s/DB delete step
   - If Ticket terminal persistence fails after Event completion is attempted, the Event update rolls back instead of leaving a completed Event with an executing Ticket
   - Added hook-injected regression coverage for Event completion and Ticket success persistence failures, proving deleted VM cleanup remains idempotent while Event stays `PENDING` and Ticket stays `EXECUTING` for retry repair
   - Verified by `go test -count=1 -run 'TestVMDeleteWorker_(EventCompletePersistFailureReturnsRetryableError|TicketSuccessPersistFailureRollsBackEventCompletion|EventCompletePersistCancellationReturnsContextError|SkipsCompletedEventWithoutExecutingProvider|SkipsFailedEventWithoutExecutingProvider|AllowsDeletingStatusAtExecutionTime)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
166. ~~**P2** — Treat approved VNC access as terminal Ticket success~~ ✅ **Done** (2026-06-19)
   - VNC approval now sets the Ticket to `SUCCESS` in the same transaction that completes the `DomainEvent`, matching the existing terminal-event repair mapping and avoiding VNC tickets lingering in the active `APPROVED` bucket with no worker to finish them
   - Console access remains backward-compatible with existing `APPROVED` VNC tickets because console status lookup still accepts both `APPROVED` and `SUCCESS`
   - Extended approval-list status group coverage so `APPROVED` remains active while `SUCCESS` is terminal, protecting the UI/API grouping behavior that motivated the change
   - Verified by `go test -count=1 -run 'TestServiceApproveVNC_(CompletesTicketAndEventWithoutAtomicWriter|RollsBackTicketWhenEventCompletionFails)|TestListApprovals_StatusGroupFiltersActiveAndTerminalTickets' -v ./internal/governance/ticketing ./internal/api/handlers`, `go test -count=1 ./internal/governance/ticketing ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `make ci-governance`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` currently fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
167. ~~**P2** — Surface batch detail projection refresh failures~~ ✅ **Done** (2026-06-19)
   - `loadBatchView` now returns projection backfill/sync errors instead of logging a warning and returning a successful detail response while `ListVMBatches` can still read stale `BatchTicket` counters
   - Missing projection backfill now treats a concurrent create constraint as a signal to retry the counter/status update, avoiding a silent stale row when another request races the repair
   - Added hook-injected regression coverage for both `GetVMBatch` projection update failure and missing-projection backfill failure, proving the API returns `500` instead of masking the persistence error
   - Verified by `go test -count=1 -run 'TestBatchHandler_GetVMBatch_ReturnsErrorWhenProjection(Sync|Backfill)Fails|TestBatchHandler_CancelVMBatch_ReturnsErrorWhenParentStatusSyncFails|TestBatchHandler_SubmitDelete_GetAndCancel' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
168. ~~**P2** — Surface power-batch idempotency status-view failures~~ ✅ **Done** (2026-06-19)
   - Power-batch idempotency now returns `500` when an existing request ID maps to a batch but `loadBatchView` cannot refresh/read its status, instead of returning a default `IN_PROGRESS` accepted response with potentially stale projection state
   - Newly submitted power batches still return `202` after the atomic create/enqueue succeeds; if the post-submit status read fails, the handler records an explicit warning and returns the conservative default status to avoid encouraging duplicate client retries after side effects are committed
   - Added hook-injected regression coverage for the duplicate power-batch request path, proving projection update failure is surfaced as `INTERNAL_ERROR`
   - Verified by `go test -count=1 -run 'TestBatchHandler_SubmitVMBatchPower_(IdempotentReturnsErrorWhenExistingViewFails|TestBatchAtomicallyEnqueues)|TestBatchHandler_GetVMBatch_ReturnsErrorWhenProjection(Sync|Backfill)Fails' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
169. ~~**P2** — Return current status for ordinary batch idempotency~~ ✅ **Done** (2026-06-19)
   - Ordinary `SubmitVMBatch` idempotency now loads the existing batch view and returns its current status instead of hard-coding `PENDING_APPROVAL`, so repeated request IDs do not misreport completed/failed batches as still waiting for approval
   - The ordinary and power-batch idempotent response paths now share one helper that surfaces missing/stale view failures as `404`/`500`, removing duplicate handler error logic while preserving request-cancel behavior
   - Added regression coverage proving a duplicate ordinary batch submit returns `COMPLETED` after the child succeeds, and returns `INTERNAL_ERROR` when the existing view cannot refresh its projection
   - Verified by `go test -count=1 -run 'TestBatchHandler_SubmitVMBatch_Idempotent(ReturnsCurrentStatus|ReturnsErrorWhenExistingViewFails|ByRequestID)|TestBatchHandler_SubmitVMBatchPower_IdempotentReturnsErrorWhenExistingViewFails' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
170. ~~**P2** — Make VM lifecycle worker execution-state persistence blocking~~ ✅ **Done** (2026-06-19)
   - Create, modify, delete, and power workers now persist `DomainEvent=PROCESSING`, child Ticket `EXECUTING`, and parent batch status/projection refresh inside one jobs-layer Ent transaction before any external K8s/provider side effect
   - Processing-state persistence failures return retryable worker errors before the provider is called, replacing the previous best-effort Ticket `EXECUTING` update path that could leave external side effects ahead of durable request state
   - Added transaction helper coverage proving Event/Ticket execution-state transitions commit together and roll back together, plus worker regression coverage for retry/cancellation paths that now intentionally remain `PROCESSING`/`EXECUTING` after the external call begins
   - Verified by `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `make ci-governance`, and `git diff --check`; real live E2E is still not complete because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
171. ~~**P2** — Block VM delete side effects when `DELETING` state cannot persist~~ ✅ **Done** (2026-06-19)
   - VM delete worker now treats the pre-delete `VM.status=DELETING` write as a blocking persistence step instead of logging and continuing into the K8s delete call
   - If the status write fails, the worker returns a retryable error before external deletion, preserving the existing `DomainEvent=PROCESSING` and Ticket `EXECUTING` state for retry while the VM row remains in its previous state
   - Added hook-injected regression coverage proving a `DELETING` status persistence failure does not call the provider delete method
   - Verified by `go test -count=1 -run 'TestVMDeleteWorker_(DeletingStatusPersistFailureDoesNotExecuteProvider|AllowsDeletingStatusAtExecutionTime|SnoozesTransientClusterErrorsWithoutFailingTicket|RetryableDeleteFailureDoesNotPersistTerminalFailure|ContextCancellationDoesNotFailEventTicketOrVM)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated and must be rechecked before release
172. ~~**P2** — Roll back delete approval when the VM row is missing~~ ✅ **Done** (2026-06-19)
   - `ApproveDeleteAndEnqueue` now requires a non-empty VM ID and checks the affected row count from sqlc `SetVMStatus`; if the expected VM row is missing, it returns an error before commit and before River `vm_delete` enqueue
   - This closes the gap where a delete approval transaction could approve the Ticket, mark the Event `PROCESSING`, and enqueue a destructive worker even though `VM.status=DELETING` was never applied to a target row
   - Added a pgx/sqlc transaction regression test proving empty/missing VM cases leave Ticket/Event pending and insert zero River jobs
   - Verified by `go test -count=1 -run 'TestApprovalAtomicWriterApproveDeleteAndEnqueue_MissingVMRollsBack' -v ./internal/usecase`, `go test -count=1 ./internal/usecase`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated and must be rechecked before release
173. ~~**P2** — Enforce power-batch retry child/event pairing~~ ✅ **Done** (2026-06-19)
   - Power-batch retry now resets a child Ticket only when `ticket_id`, `event_id`, `parent_ticket_id`, `operation_type=POWER`, and `status=FAILED` all match the same row
   - Retry Event reset now only accepts failed events, so an active or already-terminal Event cannot be rewound to `PENDING` and re-enqueued by a stale or mismatched retry request
   - Added sqlc and pgx/River transaction regression coverage proving event-ID mismatch and non-failed Event cases leave Ticket/Event state unchanged and insert zero `vm_power` jobs
   - Verified by `go test -count=1 -run 'TestQueries_ResetPowerRetryTicket|TestQueries_ResetBatchPowerRetryEvent' -v ./internal/repository/sqlc`, `go test -count=1 -run 'TestApprovalAtomicWriterRetryBatchPowerAndEnqueue_InvalidChildStateRollsBack|TestValidateBatchPowerRetryInput' -v ./internal/usecase`, `go test -count=1 ./internal/usecase ./internal/repository/sqlc`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock gates pass)
174. ~~**P2** — Make batch child status mutations pairwise and row-count checked~~ ✅ **Done** (2026-06-19)
   - Batch retry/cancel child state mutations now update each Ticket/Event pair inside one Ent transaction and require every Ticket update to match both `ticket_id` and `event_id`
   - Ticket and Event updates now check affected row counts, so a stale child ID, event mismatch, or unexpected Event status returns an error and rolls back any prior Ticket mutation in the transaction
   - Failure-marker paths for retry errors now use the same pairwise transaction helper instead of best-effort independent Ticket/Event writes
   - Added handler regression coverage proving event-ID mismatch leaves the child unchanged and Event row-count mismatch rolls back the Ticket update
   - Verified by `go test -count=1 -run 'TestBatchHandler_(MarkBatchChildFailed_RejectsTicketEventMismatch|UpdateBatchChildStatus_RollsBackTicketWhenEventDoesNotMatchExpectedStatus|RetryVMBatch_(UsesApprovalProviderSeam|PowerChildFailureMarkerRollsBackTicketOnEventPersistFailure|PowerChildUnknownOperation|PowerChildEnqueueFailure|PowerChildAtomicallyEnqueues|RollsBackChildFailureMarkerWhenEventFailWriteFails)|CancelVMBatch_RollsBackChildTicketsWhenEventCancelFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock gates pass)
175. ~~**P2** — Make batch parent reject/cancel child decisions pairwise and row-count checked~~ ✅ **Done** (2026-06-19)
   - Batch parent reject/cancel now mutates each pending child Ticket/Event pair inside the decision transaction instead of updating child Events through one bulk `IDIn` statement
   - Child Ticket updates must match `ticket_id`, `event_id`, and `status=PENDING`; child Event updates must match `event_id` and `status=PENDING`, and both updates require exactly one affected row
   - Missing or stale child Event rows now return an error and roll back parent Ticket/Event changes plus any child Ticket decision changes in the same transaction
   - Added service regression coverage for reject/cancel with a missing child Event row, alongside existing hook-failure rollback coverage
   - Verified by `go test -count=1 -run 'TestService(Reject|Cancel)BatchParent_RollsBackWhenChildEventMissing|TestService(Reject|Cancel)BatchParent_RollsBackParentAndChildrenWhenChildEventCancellationFails' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock gates pass)
176. ~~**P2** — Guard ticket decision writes against stale Ticket/Event state~~ ✅ **Done** (2026-06-19)
   - `Approve`, `Reject`, and `Cancel` now reject pending Tickets whose DomainEvent is already non-pending before entering operation-specific decision paths, preventing stale batch approvals from dispatching child work against a terminal parent Event
   - VNC approve, ordinary reject/cancel, and batch parent reject/cancel now share a guarded Ticket/Event decision helper that requires `ticket_id`, `event_id`, and `ticket.status=PENDING` plus `event.status=PENDING`, with exactly one affected row on both updates
   - Batch parent approve summary writes now also require the parent Ticket and parent Event to remain pending before persisting `EXECUTING`/`FAILED` and `PROCESSING`/`FAILED`, so a concurrent terminal parent Event returns an error instead of being overwritten
   - Added regression coverage for terminal Event rejection on VNC approve, ordinary reject/cancel, stale batch parent Event before child dispatch, and stale batch parent Event after child dispatch but before parent summary persistence
   - Verified by `go test -count=1 -run 'TestService(ApproveVNC_RejectsAlreadyTerminalEvent|Reject_RejectsAlreadyTerminalEvent|Cancel_RejectsAlreadyTerminalEvent|ApproveBatchParent_RejectsAlreadyTerminalParentEventBeforeChildDispatch|ApproveBatchParent_RollsBackParentSummaryWhenParentEvent(UpdateFails|TurnsTerminalAfterChildDispatch)|ApproveVNC_RollsBackTicketWhenEventCompletionFails|Reject_RollsBackTicketWhenEventCancellationFails|Cancel_RollsBackTicketWhenEventCancellationFails|RejectBatchParent_RollsBackWhenChildEventMissing|CancelBatchParent_RollsBackWhenChildEventMissing)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock gates pass)
177. ~~**P2** — Guard ADR-0012 atomic approvals against stale DomainEvents~~ ✅ **Done** (2026-06-19)
   - sqlc `SetDomainEventStatus` now updates only `status=PENDING` DomainEvents, preserving terminal Events instead of rewriting them to `PROCESSING`
   - Create/delete/modify atomic approval paths now report `not found or not pending` when the Event row is missing or already terminal, causing the pgx transaction to roll back the Ticket approval and skip River enqueue
   - Power atomic approval now performs a pending Event confirmation before enqueueing `vm_power`, so direct atomic-writer use cannot enqueue work for a terminal Event
   - Added SQLC row-count coverage and pgx/River transaction regressions proving terminal delete/power Events leave Tickets pending and insert zero jobs
   - Verified by `make sqlc-gen`, `go test -count=1 -run 'TestQueries_SetDomainEventStatus|TestApprovalAtomicWriterApprove(Delete|Power)AndEnqueue_(MissingVMRollsBack|TerminalEventRollsBack)' -v ./internal/repository/sqlc ./internal/usecase`, `go test -count=1 ./internal/repository/sqlc ./internal/usecase`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock gates pass)
178. ~~**P2** — Guard River worker Event/Ticket status persistence against stale rows~~ ✅ **Done** (2026-06-19)
   - River worker status helpers now advance DomainEvents only from expected non-terminal states (`PENDING`/`PROCESSING` to `PROCESSING`, and `PROCESSING` to terminal states), returning row-count errors instead of overwriting stale terminal Events
   - Ticket status updates by Event now require exactly one affected Ticket row, so missing or mismatched Ticket rows roll back Event and VM state changes inside the same transaction
   - VM create/delete/power helpers and the modify completion path now share guarded Event/Ticket persistence, including parent-batch sync error propagation after child terminal updates
   - Added regression coverage for non-pending processing attempts, missing Ticket rollback, terminal Event preservation on completed/failed persistence, and worker fixtures that prove real Event/Ticket pairing
   - Verified by `go test -count=1 -run 'Test(PersistProcessingEventAndExecutingTicketByEvent|PersistCompletedEventAndTicketByEvent|PersistFailedEventAndTicketByEvent|UpdateTicketStatusByEvent)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
179. ~~**P2** — Prevent ordinary parent batch sync from reviving terminal batches~~ ✅ **Done** (2026-06-19)
   - Parent batch status sync now updates the parent Ticket with expected-status conditions and maps mismatches to explicit row-count errors, rolling back parent Event/projection changes in the same transaction
   - Parent batch DomainEvents now use the guarded status helper, so ordinary worker-driven child completion cannot rewrite a terminal parent Event to another terminal state or back to `PROCESSING`
   - Explicit retry now reopens the approved parent/event inside the same transaction as failed-child resets and dispatcher insertion; the only remaining reconciliation path is narrowly limited to `FAILED -> EXECUTING` / `FAILED -> PROCESSING` when active children prove a stale aggregation race
   - Added regression coverage for stale terminal parent Event preservation, stale terminal parent Ticket preservation, and narrow reconciliation of a failed parent with active children
   - Verified by `go test -count=1 -run 'TestSyncParentBatchStatus|TestUpdateTicketStatusByEvent(PropagatesParentBatchSyncFailure|RollsBackParentBatchSyncOnProjectionFailure)|TestBatchHandler_RetryVMBatch_RetriesFailedDeleteChild|TestBatchHandler_CancelVMBatch_ReturnsErrorWhenParentStatusSyncFails' -v ./internal/jobs ./internal/api/handlers`, `go test -count=1 ./internal/jobs ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
180. ~~**P2** — Guard batch child dispatch-failure marking against stale Ticket/Event state~~ ✅ **Done** (2026-06-19)
   - Batch parent approval now marks child dispatch failures through the guarded pending Ticket/Event decision helper instead of unconditional `UpdateOneID` writes
   - Dispatch-failure marking now requires the child Ticket and child DomainEvent to remain `PENDING`, returning row-count errors and rolling back child Ticket decision fields when either row is stale
   - Added regression coverage for stale child Events during dispatch-failure marking, alongside the existing persistence-failure rollback case
   - Verified by `go test -count=1 -run 'TestServiceApproveBatchParent_(StopsWhenChildFailurePersistenceFails|RejectsStaleChildEventWhenMarkingDispatchFailure|RollsBackParentSummaryWhenParentEventTurnsTerminalAfterChildDispatch|RejectsAlreadyTerminalParentEventBeforeChildDispatch)' -v ./internal/governance/ticketing`, `go test -count=1 ./internal/governance/ticketing`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
181. ~~**P2** — Allow power-batch retry to reopen rejected children atomically~~ ↩ **Superseded** (2026-07-19)
   - `REJECTED` is an approval decision, not an execution failure. Reopening it through the execution-retry endpoint could bypass the approval boundary.
   - Current handlers and SQL CAS reset only `FAILED` children; rejected requests require a new batch and approval decision.
182. ~~**P2** — Make create-batch retry review inputs atomic with child reset~~ ✅ **Done** (2026-06-19)
   - Create-batch retry now persists parent `selected_cluster_id` and `selected_storage_class` in the same Ent transaction that resets retryable child Ticket/Event rows
   - A child reset failure now rolls back the parent retry-review fields, child Ticket reset, and child Event reset together instead of leaving a failed retry with partially updated parent review inputs
   - Added handler regression coverage that injects a child Event reset failure after review input submission and proves the approval router is not called while parent and child rows remain unchanged
   - Verified by `go test -count=1 -run 'TestBatchHandler_RetryVMBatch_(UsesReviewBodyForFailedCreateBatch|RollsBackReviewInputsWhenChildResetFails|RequiresReviewWhenFailedCreateBatchHasNoSelectedCluster)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
183. ~~**P2** — Guard batch child failure markers against stale terminal children~~ ✅ **Done** (2026-06-19)
   - Power-retry validation failure marking acquires the parent mutation lock and requires the exact parent/ticket/event pairing to remain `FAILED` before consuming one logical attempt.
   - Matching child DomainEvents must remain `PENDING`, `FAILED`, or `CANCELLED`; stale ticket/event state returns a conflict and rolls back the transaction.
   - Public handler and atomic-writer regressions cover invalid operations, stale state, attempt exhaustion, and enqueue rollback without calling private helpers directly.
184. ~~**P2** — Make power worker success VM/Event/Ticket persistence atomic~~ ✅ **Done** (2026-06-19)
   - Power worker success handling now persists the observed VM status, polling tier/interval, resource version, DomainEvent `COMPLETED`, Ticket `SUCCESS`, and parent-batch sync inside one Ent transaction
   - VM status persistence failures keep the existing retryable `persist VM status after ...` error classification, while Event/Ticket completion failures now roll back the VM status/RV update instead of leaving a half-completed power job
   - Updated power worker regressions to prove Event completion errors and cancellations leave the DB VM row at its pre-power status even though the provider-side power operation already executed
   - Verified by `go test -count=1 -run 'TestVMPowerWorker_(UsesObservedLiveStatusAfterStop|StopStatusPersistFailureReturnsRetryableError|StopEventCompletePersistFailureReturnsRetryableError|EventCompletePersistCancellationReturnsContextError|FinalPowerFailurePersistsTerminalFailure)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
185. ~~**P2** — Make delete worker success cleanup/Event/Ticket persistence atomic~~ ✅ **Done** (2026-06-19)
   - Delete worker success handling now persists VM row cleanup, DomainEvent `COMPLETED`, Ticket `SUCCESS`, and parent-batch sync in one transaction when the hard-delete succeeds, so Event/Ticket completion failures roll back the VM row deletion
   - If the K8s delete already succeeded but DB hard-delete fails, the worker now rolls back that failed delete transaction, keeps the pre-existing `DELETING` tombstone, and completes Event/Ticket in a fresh transaction instead of relying on an aborted PostgreSQL transaction
   - Updated delete worker regressions to prove Event completion errors, Ticket success errors, and cancellations keep the DB VM row at `DELETING` while the provider-side VM is already deleted; added hard-delete fallback coverage proving tombstone completion still succeeds
   - Verified by `go test -count=1 -run 'TestVMDeleteWorker_(EventCompletePersistFailureReturnsRetryableError|TicketSuccessPersistFailureRollsBackEventCompletion|EventCompletePersistCancellationReturnsContextError|HardDeleteFailureCompletesWithTombstone|SkipsK8sDeleteWhenStatusNotFound|AllowsDeletingStatusAtExecutionTime)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
186. ~~**P2** — Make failed worker terminal persistence include parent-batch sync atomically~~ ✅ **Done** (2026-06-19)
   - Failed Event/Ticket persistence now writes optional VM `FAILED` status, DomainEvent `FAILED`, Ticket `FAILED`/reject reason, and parent-batch sync inside one jobs-layer Ent transaction
   - Parent batch sync failures now roll back the child failed Event/Ticket and VM status mutation instead of leaving a failed child with stale parent Ticket/Event/projection state
   - Added a helper-level regression proving parent projection persistence failure keeps the VM at its pre-failure status and leaves child Event/Ticket plus parent aggregates unchanged
   - Verified by `go test -count=1 -run 'TestPersistFailedEvent(AndTicketByEvent(RejectsTerminalEvent|PropagatesEventFailure|RollsBackEventOnTicketFailure)|TicketAndVMByEventRollsBackChildOnParentSyncFailure)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
187. ~~**P2** — Guard pending-adoption admin decisions against stale status writes~~ ✅ **Done** (2026-06-19)
   - Reject now marks a candidate `REJECTED` through `UpdateOneID(...).Where(status=PENDING)`, mapping stale conditional-update misses to the existing `409 PENDING_ADOPTION_NOT_PENDING` response instead of overwriting a concurrent terminal decision
   - Adopt now applies the same `status=PENDING` guard to the final in-transaction `ADOPTED` update after VM row creation, so a concurrent decision rolls back the newly created VM inventory row instead of committing a partial adoption
   - Added handler regressions that inject concurrent `ADOPTED`/`REJECTED` status changes immediately before the final Ent update, proving stale reject/adopt requests preserve the concurrent decision and skip audit/VM side effects
   - Verified by `go test -count=1 -run 'Test(RejectPendingAdoption_(RejectsPendingRowAndAudits|RejectsOnlyPendingRows|DoesNotOverwriteConcurrentDecision)|AdoptPendingAdoption_(AdoptsLiveVMAndAudits|RejectsExistingVMRow|DoesNotCommitVMWhenDecisionBecomesStale))' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
188. ~~**P2** — Prevent adoption discovery refresh from mutating concurrently resolved candidates~~ ✅ **Done** (2026-06-19)
   - Pending-adoption discovery refresh now updates existing candidates through `UpdateOneID(...).Where(status=PENDING)` so a concurrent adopt/reject/expire decision cannot have labels, resource type, or `discovered_by` overwritten by a stale scanner refresh
   - Conditional-update misses are treated as `SkippedAlreadyResolved`, preserving scanner idempotency and keeping one concurrently resolved candidate from failing the whole namespace scan
   - Added service regression coverage that injects a concurrent `REJECTED` transition immediately before the refresh update and proves the old labels/discovery actor remain unchanged
   - Verified by `go test -count=1 -run 'TestAdoptionDiscoveryService_(DiscoversLabelledVMs|DoesNotRefreshConcurrentResolvedCandidate)' -v ./internal/service`, `go test -count=1 ./internal/service`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
189. ~~**P2** — Prevent live VM refresh from overwriting delete-owned state~~ ✅ **Done** (2026-06-19)
   - API live VM refresh now skips rows already in `DELETING` and persists observed/unavailable state only when the current DB row is still not `DELETING`, reloading the current row for responses when the conditional update misses
   - The `vm_status_sync` worker now applies the same non-`DELETING` guard to observed-status writes, missing-VM writes, and stale resourceVersion resets, stopping the poll chain instead of reviving a VM that a delete workflow has just claimed
   - Added API and worker regressions that inject a concurrent `DELETING` transition immediately before live/status-sync persistence and prove `RUNNING`/`STOPPED` observations plus resource versions do not overwrite the delete-owned state
   - Verified by `go test -count=1 -run 'TestGetVM_(RefreshesStatusFromLiveCluster|DoesNotOverwriteConcurrentDeletingStatus|MarksUnknownWhenLiveVMIsMissing)|TestListVMs_(RefreshesStatusFromLiveCluster|MarksUnknownWhenClusterIsUnavailable)' -v ./internal/api/handlers`, `go test -count=1 -run 'TestVMStatusSyncWorkerWork_(PersistsObservedStatusAndResourceVersion|DoesNotOverwriteConcurrentDeletingStatus|MarksMissingVMNotFound|ClearsCachedResourceVersionOnExpiredList|SkipsRowsThatShouldNotPollCluster)' -v ./internal/jobs`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E is still externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
190. ~~**P2** — Prevent VM modify/power workers from mutating delete-owned state~~ ✅ **Done** (2026-06-19)
   - `vm_modify` and `vm_power` now re-check the persisted VM row before executing provider/K8s side effects and cancel terminally when the row is already `DELETING` or absent, so stale queued lifecycle jobs do not mutate resources already claimed by delete
   - Completed modify/power persistence now writes VM status and polling metadata only while the current DB row is not `DELETING`; conditional-update misses are treated as delete-owned/absent rows while Event/Ticket completion still commits
   - Added worker regressions for both pre-existing `DELETING` rows and concurrent `DELETING` transitions injected immediately before completed-state persistence, proving provider calls are skipped before deletion and completed writeback cannot revive delete-owned rows
   - Verified by `go test -count=1 -run 'TestVMModifyWorker_(ExecutesLiveMemoryUpdateAndPersistsStatus|CancelsWhenVMIsDeletingBeforeMutation|DoesNotOverwriteConcurrentDeletingStatusAfterMutation|SkipsCompletedEventWithoutExecutingProvider)' -v ./internal/jobs`, `go test -count=1 -run 'TestVMPowerWorker_(UsesObservedLiveStatusAfterStop|CancelsWhenVMIsDeletingBeforeProviderCall|DoesNotOverwriteConcurrentDeletingStatusAfterProviderSuccess|SkipsCompletedEventWithoutExecutingProvider)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-build`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
191. ~~**P2** — Guard delete worker claim against stale executable status~~ ✅ **Done** (2026-06-19)
   - `vm_delete` now claims destructive execution with `UpdateOneID(...).Where(status IN delete-executable statuses).SetStatus(DELETING)` instead of unconditionally writing `DELETING` after an earlier status read
   - If the conditional claim misses because live/status-sync or another worker changed the row to a non-executable state such as `RUNNING`, the delete job cancels before any provider/K8s delete call and marks the stale delete Event/Ticket failed
   - Added a regression that injects `RUNNING` immediately before the delete claim and proves the provider delete is not called, the VM row remains `RUNNING`, and the stale delete Event/Ticket become terminal failed
   - Verified by `go test -count=1 -run 'TestVMDeleteWorker_(SkipsK8sDeleteWhenStatusNotFound|AttemptsK8sDeleteWhenStatusUnknown|AllowsDeletingStatusAtExecutionTime|DoesNotDeleteWhenStatusBecomesNonExecutableBeforeClaim|HardDeleteFailureCompletesWithTombstone)' -v ./internal/jobs`, `go test -count=1 -run 'TestVMDeleteWorker_(SkipsK8sDeleteWhenStatusNotFound|AllowsDeletingStatusAtExecutionTime|DoesNotDeleteWhenStatusBecomesNonExecutableBeforeClaim|DeletingStatusPersistFailureDoesNotExecuteProvider|RetryableDeleteFailureDoesNotPersistTerminalFailure|FinalDeleteFailurePersistsTerminalFailure|SkipsK8sDeleteWhenStatusNotFound)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-build`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
192. ~~**P2** — Prevent VM create terminal persistence from reviving delete-owned rows~~ ✅ **Done** (2026-06-19)
   - Create completion now writes the DB VM status through a non-`DELETING` conditional update, so an idempotent or successful create retry can complete Event/Ticket state without changing a row already claimed by deletion
   - Create final-failure persistence now uses a create-specific failed terminal helper that skips the VM `FAILED` write when the row is `DELETING`, while preserving the delete worker's existing behavior of moving its own failed delete claim to `FAILED`
   - Added create worker regressions for concurrent `DELETING` before completed-state persistence and before final-failure persistence, proving Event/Ticket terminal states commit while the VM remains delete-owned
   - Verified by `go test -count=1 -run 'TestVMCreateWorker_(SkipsK8sCreateWhenEventLabeledVMAlreadyExists|DoesNotOverwriteConcurrentDeletingStatusOnCompletedPersistence|FinalCreateFailurePersistsTerminalFailure|FinalCreateFailureDoesNotOverwriteConcurrentDeletingStatus|FinalCreateFailureRollsBackVMOnEventPersistFailure)' -v ./internal/jobs`, `go test -count=1 -run 'TestVMDeleteWorker_(FinalDeleteFailurePersistsTerminalFailure|FinalDeleteFailureRollsBackVMOnEventPersistFailure)' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, and `make ci-go-build`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
193. ~~**P2** — Restrict external cohort RBAC to managed RoleBindings~~ ✅ **Done** (2026-06-19)
   - External cohort reconciliation now only reuses an existing `RoleBinding` when it belongs to the user, was created by `system:external-cohort-mapper`, and matches the desired role/scope/environment tuple
   - Drifted mapper-owned bindings are repaired in place, while grant rows that point at unmanaged/manual bindings are moved to a newly created managed binding without deleting the manual binding
   - Stale grant deletion now conditionally deletes the referenced `RoleBinding` only when it is mapper-owned and belongs to the grant user, preventing a corrupt grant from deleting an administrator-created binding
   - Added regressions for unmanaged binding preservation on grant delete, unmanaged grant-binding replacement, managed binding drift repair, and the existing transaction rollback paths
   - Verified by `go test -count=1 -run 'TestExternalAuthService_ReconcileManagedBinding(RollsBackRoleBindingOnGrantCreateFailure|RollsBackGrantDeleteOnRoleBindingDeleteFailure|PreservesUnmanagedRoleBindingOnGrantDelete|ReplacesUnmanagedGrantBinding|RepairsManagedRoleBindingDrift)' -v ./internal/service`, `go test -count=1 ./internal/service`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
194. ~~**P2** — Require mapper ownership for role-binding managed-state projection~~ ✅ **Done** (2026-06-19)
   - Admin user role-binding API projection now reports `managed=true` for `external_cohort` only when the `RoleBinding` belongs to the listed user, was created by `system:external-cohort-mapper`, and has a matching same-user `ExternalCohortGrant`
   - A stale or corrupt grant that points at an administrator-created binding no longer causes that manual binding to appear as externally managed in list responses
   - Added a handler regression covering both sides: a manual binding with a corrupt grant stays unmanaged, while a mapper-owned binding with a matching grant still reports `managed_source=external_cohort`
   - Verified by `go test -count=1 -run 'TestListUserRoleBindings_ManagedStateRequiresMapperOwnedGrant|TestUserRoleBinding_IncludesRoleAndScopeDisplayNames|TestAdminUserRoleBindingAndAuthProviderCRUD' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
195. ~~**P2** — Allow external cohort mapping PATCH to clear scoped RBAC targets~~ ✅ **Done** (2026-06-19)
   - `UpdateAuthProviderCohortMapping` now preserves PATCH field-presence semantics for `scope_type`, `scope_id`, and `allowed_environments` instead of relying on generated zero-value strings
   - Admins can clear a mapping's scoped target by sending `scope_id: ""`, and setting `scope_type` to `global` without a `scope_id` now clears any stale scoped ID instead of leaving `global + old scope_id`
   - Allowed-environment-only updates still preserve the existing scope tuple, preventing accidental permission-target drift during environment edits
   - Added a handler regression that first verifies an allowed-environment update preserves `system/system-old`, then verifies a global scope update clears the persisted and returned `scope_id`
   - Verified by `go test -count=1 -run 'Test(UpdateAuthProviderCohortMapping_ClearsScopeWhenRequested|AuthProviderStage2CFlow)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
196. ~~**P2** — Revoke external cohort grants when deleting cohort mappings~~ ✅ **Done** (2026-06-19)
   - `DeleteAuthProviderCohortMapping` now runs inside the existing Ent transaction helper and reconciles existing `ExternalCohortGrant` rows that reference the deleted mapping ID
   - Grants backed only by the deleted mapping are removed immediately, and their mapper-owned same-user `RoleBinding` is conditionally deleted in the same transaction
   - Grants backed by multiple source mappings keep their managed `RoleBinding` and have `source_mapping_ids` reduced to the remaining mapping IDs, so shared role/scope/environment bindings are not over-revoked
   - Added regressions for single-source revocation, shared-source preservation, and rollback when managed RoleBinding cleanup fails after the mapping delete
   - Verified by `go test -count=1 -run 'TestDeleteAuthProviderCohortMapping_(ReconcilesExistingGrants|RollsBackWhenGrantCleanupFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
197. ~~**P2** — Migrate external cohort grants when updating cohort mappings~~ ✅ **Done** (2026-06-19)
   - `UpdateAuthProviderCohortMapping` now updates the mapping and reconciles affected `ExternalCohortGrant` rows inside one Ent transaction, so changing role/scope/environment no longer leaves existing users on stale managed RBAC until their next external-auth reconciliation
   - Existing grants that only referenced the updated mapping are moved to the new binding key with a newly created mapper-owned `RoleBinding`, while the stale mapper-owned same-user binding is conditionally deleted
   - Shared old grants keep their remaining source mappings, and updates that merge into an already-existing target grant only add the moved mapping ID instead of creating duplicate grants or deleting shared bindings
   - Added regressions for single-source migration, shared-source merge into an existing target grant, and transaction rollback when target RoleBinding creation fails after the mapping update
   - Verified by `go test -count=1 -run 'TestUpdateAuthProviderCohortMapping_(ClearsScopeWhenRequested|MigratesExistingGrantToUpdatedBinding|MergesIntoExistingTargetGrant|RollsBackWhenGrantMigrationFails)|TestDeleteAuthProviderCohortMapping_(ReconcilesExistingGrants|RollsBackWhenGrantCleanupFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
198. ~~**P2** — Revoke auth sessions when deleting users~~ ✅ **Done** (2026-06-19)
   - `DeleteUser` now calls the auth session manager from the same handler transaction callback after deleting dependent Ent rows and the user row, so successful admin user deletion bumps the user's session version instead of leaving existing JWTs on the previous version
   - If session revocation fails, the Ent transaction returns an error and rolls back the user delete plus dependent `ExternalCohortGrant`, `RoleBinding`, and `UserPreference` cleanup
   - Added regressions for successful session-version increment on delete and rollback preservation when the session store is unavailable, alongside the existing dependent-row rollback coverage
   - Verified by `go test -count=1 -run 'TestDeleteUser_(RevokesSessionsOnSuccess|RollsBackWhenSessionRevocationFails|RollsBackAssociatedRowsWhenDeleteStepFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
199. ~~**P2** — Block role deletion while external cohort mappings reference the role~~ ✅ **Done** (2026-06-19)
   - `DeleteRole` now checks `ExternalCohortMapping.role_id` before deletion, so a role cannot be removed while external cohort RBAC mappings still point at it
   - The handler reuses the existing `ROLE_IN_USE` conflict response for mapping-backed references, matching the existing bound-RoleBinding delete guard
   - Added a regression covering a role with no `RoleBinding` rows but an external cohort mapping reference, proving the role and mapping both remain after the rejected delete
   - Verified by `go test -count=1 -run 'Test(DeleteRoleRejectsExternalCohortMappingReference|AdminUserRoleBindingAndAuthProviderCRUD|UpdateRole_RollsBackWhenSessionRevocationFails)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
200. ~~**P2** — Clean external cohort state when deleting auth providers~~ ✅ **Done** (2026-06-19)
   - `DeleteAuthProvider` now runs inside the handler transaction helper, keeps the existing linked-user conflict guard inside the transaction, and deletes provider-owned external cohort grants, mappings, and cohort rows before deleting the provider
   - Residual provider grants are removed through the existing mapper-owned same-user RoleBinding cleanup helper, so stale external-cohort permissions do not survive provider deletion while administrator-created bindings remain protected by the conditional delete
   - Added regressions for successful provider deletion cleaning `ExternalCohort`, `ExternalCohortMapping`, `ExternalCohortGrant`, and mapper-owned `RoleBinding` rows, plus rollback when grant RoleBinding cleanup fails
   - Verified by `go test -count=1 -run 'TestDeleteAuthProvider(ReturnsConflictWhenUsersRemainLinked|_CleansExternalCohortState|_RollsBackWhenCohortGrantCleanupFails)|TestAdminUserRoleBindingAndAuthProviderCRUD' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
201. ~~**P2** — Invalidate linked sessions when disabling auth providers~~ ✅ **Done** (2026-06-19)
   - `AuthSessionManager.ValidateClaims` now rejects JWTs for users linked to a missing or disabled auth provider, so already-issued external-user tokens no longer survive provider disablement
   - `UpdateAuthProvider` now runs the provider update through the handler transaction helper and revokes sessions for users linked to the provider when `enabled` transitions from true to false; revocation failure rolls back the provider update
   - Added service coverage for disabled-provider claims rejection plus handler coverage for session-version increment on provider disable and rollback when linked-user session revocation fails
   - Verified by `go test -count=1 -run 'TestAuthSessionManagerValidateClaimsRejects(DisabledAuthProvider|StaleSessionVersion)' -v ./internal/service`, `go test -count=1 -run 'TestUpdateAuthProvider_(DisablingRevokesLinkedUserSessions|RollsBackWhenLinkedSessionRevocationFails)|TestAuthProviderConfigSecretsStayEncryptedAndRevealOnlyForEditableResponses' -v ./internal/api/handlers`, `go test -count=1 ./internal/service`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
202. ~~**P2** — Invalidate sessions after external cohort grant cleanup~~ ✅ **Done** (2026-06-19)
   - `DeleteAuthProvider`, `UpdateAuthProviderCohortMapping`, and `DeleteAuthProviderCohortMapping` now collect users whose provider-owned external cohort grants are deleted, moved, merged, or source-pruned, and revoke those users' auth sessions from the same handler transaction callback
   - External cohort cleanup helpers now return compacted affected user IDs while preserving mapper-owned same-user RoleBinding deletion guards, so stale JWT permissions are invalidated after admin-driven automatic RBAC cleanup instead of surviving until token expiry
   - Session revocation failures now abort and roll back mapping update/delete cleanup, preventing database RBAC from changing while old JWT permission claims remain usable
   - Added regressions for provider deletion session-version increment, mapping update/delete session-version increment, and mapping update/delete rollback when the auth session store is unavailable
   - Verified by `go test -count=1 -run 'Test(DeleteAuthProvider_(CleansExternalCohortState|RollsBackWhenCohortGrantCleanupFails|RevokesAffectedExternalCohortGrantSessions)|UpdateAuthProviderCohortMapping_(ClearsScopeWhenRequested|MigratesExistingGrantToUpdatedBinding|MergesIntoExistingTargetGrant|RollsBackWhenGrantMigrationFails|RevokesAffectedUserSessions|RollsBackWhenSessionRevocationFails)|DeleteAuthProviderCohortMapping_(ReconcilesExistingGrants|RollsBackWhenGrantCleanupFails|RevokesAffectedUserSessions|RollsBackWhenSessionRevocationFails))' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 ./internal/service`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
203. ~~**P2** — Invalidate sessions when external auth reconciliation changes RBAC~~ ✅ **Done** (2026-06-19)
   - `reconcileExternalCohortRBAC` now reports whether it actually created, updated, replaced, or deleted mapper-owned RoleBindings, while ignoring non-permission metadata refreshes such as `last_applied_at`
   - External login now revokes the affected user's auth sessions before issuing the new login response when external cohort reconciliation changes RBAC, so old JWTs with stale permission claims are invalidated and the newly issued token carries the incremented session version
   - Directory sync worker wiring now shares the production auth session manager and revokes sessions inside each record transaction when manual import or scheduled enrichment changes automatic RBAC for an existing user
   - Added regressions for `RBACChanged` semantics, credential-mode login session-version increment after stale grant removal, and scheduled enrichment session-version increment after automatic RBAC creation
   - Verified by `go test -count=1 -run 'TestExternalAuthService_UpsertExternalUser_ReconcilesManagedBindings' -v ./internal/service`, `go test -count=1 -run 'TestSubmitLoginAuthProvider_CredentialMode_RBACChangeRevokesExistingSessions' -v ./internal/api/handlers`, `go test -count=1 -run 'TestDirectorySyncWorker_RevokesSessionsWhenScheduledEnrichmentChangesRBAC' -v ./internal/jobs`, `go test -count=1 ./internal/service`, `go test -count=1 ./internal/api/handlers`, `go test -count=1 ./internal/jobs`, `go test -count=1 ./internal/app/modules`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and `make ci-governance`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
204. ~~**P2** — Clarify implemented JWT session-version revocation in design governance~~ ✅ **Done** (2026-06-19)
   - Reconciled auth/session docs so JWT session-version revocation is documented as implemented, while productized session listing and administrator session-management APIs remain RFC-0008 future scope
   - Disambiguated VNC active revocation from JWT session revocation in Stage 6 status text, preserving the noVNC/VNC hardening boundary as V2+ scope
   - Updated `check_design_doc_governance.sh` to reject stale claims that active JWT revocation is wholly deferred and to require the implemented session-version revocation baseline in phase/current-state docs
   - Verified by `bash -n docs/design/ci/scripts/check_design_doc_governance.sh`, `bash docs/design/ci/scripts/check_design_doc_governance.sh`, and stale-phrase `rg` checks; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
205. ~~**P2** — Prevent disabled roles from widening environment visibility~~ ✅ **Done** (2026-06-19)
   - `resolveNamespaceVisibility` now filters RoleBindings through enabled Role edges before computing `allowed_environments`, preventing disabled unrestricted bindings from making another enabled restricted role appear unrestricted
   - `namespaceVisibilityFromRoleBindings` also ignores disabled loaded Role edges, so helper-level callers and tests fail closed when role state is available
   - Added regression coverage for the mixed case of an enabled test-only binding plus a disabled unrestricted binding, including a Postgres-backed Ent query path
   - Verified by `go test -count=1 -run 'Test(NamespaceVisibilityFromRoleBindings|ResolveNamespaceVisibility_IgnoresDisabledRoles)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
206. ~~**P2** — Hide disabled roles from active user role summaries~~ ✅ **Done** (2026-06-19)
   - `loadRoleNames` now ignores loaded disabled Role edges, aligning admin user summaries with login/current-user permission resolution that already excludes disabled roles
   - Added focused coverage proving a user with mixed enabled and disabled bindings only reports enabled role names in the summary helper
   - Verified by `go test -count=1 -run 'TestLoadRoleNames_IgnoresDisabledRoles' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
207. ~~**P2** — Stabilize auth role-name claims for multi-scope bindings~~ ✅ **Done** (2026-06-19)
   - `loadUserRolesAndPermissions` now returns sorted, deduplicated active role names alongside flattened permissions, preventing duplicate JWT/UserInfo role names when the same role is bound through multiple scopes
   - Login and `/auth/me` now share that stable active role-name set instead of rebuilding names from raw RoleBinding edges at each call site
   - Added DB-backed regression coverage proving `/auth/me` returns sorted unique active roles and skips a disabled bound role
   - Verified by `go test -count=1 -run 'TestGetCurrentUser_(IncludesPermissions|DeduplicatesAndSortsActiveRoleNames)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
208. ~~**P2** — Normalize supported permission keys before issuing auth claims~~ ✅ **Done** (2026-06-19)
   - `loadUserRolesAndPermissions` now trims permission keys and drops empty or unsupported legacy permissions before writing `/auth/me` or JWT claims, while preserving sorted de-duplicated supported permissions
   - Extended `/auth/me` coverage so a role containing duplicates, whitespace, and a legacy unsupported permission still returns only the canonical supported permission set
   - Verified by `go test -count=1 -run 'TestGetCurrentUser_(IncludesPermissions|DeduplicatesAndSortsActiveRoleNames)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
209. ~~**P2** — Ignore disabled roles in user list projection and role search~~ ✅ **Done** (2026-06-19)
   - `toGeneratedUser` now trims and de-duplicates only enabled role names, keeping admin user-list summaries aligned with auth claims and role-binding summaries
   - User search role predicates now require `Role.enabled = true`, so `roles:` filters and free-text role matching no longer return users whose only matching binding is disabled
   - Added focused coverage for disabled-role projection and DB-backed role-search behavior
   - Verified by `go test -count=1 -run 'Test(ToGeneratedUser_IgnoresDisabledRoles|ApplyUserSearch_SupportsMailAndRolesAliases)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
210. ~~**P2** — Prevent disabled users from being added as system members~~ ✅ **Done** (2026-06-19)
   - `ListSystemMemberCandidates` now filters to enabled users while keeping the administrator user list capable of showing disabled accounts for audit and recovery workflows
   - `AddSystemMember` now rejects disabled users with `USER_DISABLED` before creating a `resource_role_bindings` row
   - Extended DB-backed member-management coverage so disabled users are absent from addable candidates and direct API adds do not persist a binding
   - Verified by `go test -count=1 -run 'Test(ListSystemMemberCandidates_ExcludesExistingMembersAndSupportsSearch|AddSystemMember_RejectsDisabledUser)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
211. ~~**P2** — Reject new RBAC assignments to disabled roles~~ ✅ **Done** (2026-06-19)
   - `CreateUserRoleBinding` now rejects disabled roles with `ROLE_DISABLED` before writing a new user role binding
   - External cohort mapping create/update now rejects disabled roles with `ROLE_DISABLED`, preventing future directory sync or mapping migration from creating managed bindings that silently activate when a role is later re-enabled
   - Existing bindings and mappings remain visible for audit and cleanup; the guard only blocks new assignments to disabled roles
   - Verified by `go test -count=1 -run 'Test(CreateUserRoleBinding_RejectsDisabledRole|AuthProviderCohortMapping_RejectsDisabledRole)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
212. ~~**P2** — Reject new role bindings for disabled users~~ ✅ **Done** (2026-06-19)
   - `CreateUserRoleBinding` now rejects disabled target users with `USER_DISABLED` before duplicate checks or session revocation, preventing dormant RBAC grants from silently activating when a user is later re-enabled
   - Existing disabled-user bindings remain visible for audit and cleanup; the guard only blocks new user role assignments
   - Added DB-backed coverage proving the direct role-binding API rejects disabled users and persists no `role_bindings` row
   - Verified by `go test -count=1 -run 'TestCreateUserRoleBinding_RejectsDisabled(User|Role)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, and `git diff --check`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml`
213. ~~**P2** — Skip disabled-role external cohort mappings during RBAC reconciliation~~ ✅ **Done** (2026-06-19)
   - `buildDesiredExternalCohortBindings` now resolves the provider's mapped role IDs and only treats mappings to enabled roles as desired RBAC, preventing external login or directory sync from creating managed grants for disabled roles
   - Existing mapper-owned grants and role bindings whose source mapping now points at a disabled role are pruned on the next matching external cohort reconciliation instead of remaining dormant until the role is re-enabled
   - Added DB-backed coverage proving a disabled-role mapping creates no `role_bindings`/`external_cohort_grants`, and that an existing managed grant/binding for such a mapping is deleted during reconciliation
   - Verified by `go test -count=1 -run 'TestExternalAuthService_(UpsertExternalUser_ReconcilesManagedBindings|ReconcileManagedBindingSkipsDisabledRoleMapping|ReconcileManagedBindingDeletesGrantForDisabledRoleMapping)' -v ./internal/service`, `go test -count=1 ./internal/service`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
214. ~~**P2** — Persist external disabled-user results and clear automatic RBAC~~ ✅ **Done** (2026-06-19)
   - `UpsertExternalUser` now treats disabled external-auth results as having no desired RBAC cohorts, so directory-authoritative syncs persist the disabled user state while deleting existing provider-owned external cohort grants and mapper-owned role bindings instead of creating dormant access
   - External login finalization now commits the disabled user update and session-version revocation before returning `USER_DISABLED`, preventing the previous rollback path from leaving old JWTs valid after the IdP reported the user disabled
   - Added DB-backed service coverage for disabled external-user creation and cleanup, plus credential-login handler coverage proving a 403 response still persists `users.enabled=false`, deletes managed cohort RBAC, and increments the session version
   - Verified by `go test -count=1 -run 'TestExternalAuthService_(UpsertExternalUserSkipsRBACForDisabledUser|UpsertExternalUserDeletesManagedRBACWhenUserDisabled|ReconcileManagedBindingSkipsDisabledRoleMapping|ReconcileManagedBindingDeletesGrantForDisabledRoleMapping)' -v ./internal/service`, `go test -count=1 -run 'TestSubmitLoginAuthProvider_CredentialMode_(DisabledResultPersistsAndRevokesSessions|RBACChangeRevokesExistingSessions)' -v ./internal/api/handlers`, `go test -count=1 ./internal/service`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
215. ~~**P2** — Prevent directory sync from granting RBAC to disabled matched users~~ ✅ **Done** (2026-06-19)
   - Manual directory sync updates and scheduled enrichment now derive desired external-cohort RBAC from the matched Shepherd user's `enabled` state, so locally disabled users receive no new mapper-owned role bindings even when directory records still carry matching cohorts
   - Existing provider-owned external cohort grants and mapper-owned role bindings for disabled matched users are pruned during the next manual sync or scheduled enrichment, with `RBACChanged=true` so worker-level session revocation remains triggered
   - Added DB-backed service coverage for both `ApplyRecord` and `ApplyEnrichmentRecord`, proving disabled matched users stay disabled while managed cohort grants and bindings are removed
   - Verified by `go test -count=1 -run 'TestDirectorySyncServiceApply(Record_ClearsManagedRBACForDisabledMatchedUser|EnrichmentRecord_ClearsManagedRBACForDisabledMatchedUser|Record_ReconcilesObservedCohortsAndBindings)' -v ./internal/service`, `go test -count=1 ./internal/service`, `go test -count=1 ./internal/api/handlers`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
216. ~~**P2** — Block directory sync execution for disabled auth providers~~ ✅ **Done** (2026-06-19)
   - `DirectorySyncWorker` now fails queued directory sync jobs before adapter resolution when the target `AuthProvider` is disabled, preventing old or manual jobs from mutating users or automatic RBAC after provider deactivation
   - Scheduled directory enrichment scanning now only queries enabled auth providers and defensively skips disabled rows before enqueue, so disabled providers cannot create new `directory_sync_jobs`
   - Added DB-backed worker/scheduler coverage proving disabled providers persist a failed sync job without creating users and enqueue no scheduled enrichment job
   - Verified by `go test -count=1 -run 'TestDirectorySyncWorker_FailurePathsPersistJobFailure' -v ./internal/jobs`, `go test -count=1 -run 'TestDirectoryEnrichmentScheduleScanWorker_SkipsDisabledAuthProviders' -v ./internal/jobs`, `go test -count=1 ./internal/jobs`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
217. ~~**P2** — Reject disabled auth providers at directory sync API entrypoints~~ ✅ **Done** (2026-06-19)
   - Directory preview and manual directory sync API handlers now return `409 AUTH_PROVIDER_DISABLED` before decrypting provider config, calling provider adapters, or enqueueing jobs when the target `AuthProvider` is disabled
   - Directory schedule status now reports disabled providers as `supported=true, enabled=false` without building the provider-owned schedule plan, aligning admin visibility with the scheduler worker's enabled-provider filter
   - OpenAPI main/compat/embed specs and generated Go/TypeScript API artifacts now document the new 409 response for directory preview and sync operations
   - Added DB-backed handler coverage proving disabled providers do not call preview/sync adapters, do not create `directory_sync_jobs`, and do not display active schedule status
   - Verified by `go test -count=1 -run 'Test(PreviewAuthProviderDirectory_RejectsDisabledAuthProvider|SyncAuthProviderDirectory_RejectsDisabledAuthProviderWithoutCreatingJob|GetAuthProviderDirectorySchedule_DisabledAuthProviderReturnsDisabledStatus)' -v ./internal/api/handlers`, `go test -count=1 -run 'Test(PreviewAuthProviderDirectory|SyncAuthProviderDirectory|GetAuthProviderDirectorySchedule)' -v ./internal/api/handlers`, `go test -count=1 ./internal/api/handlers`, `make ci-api-sync-local`, `make ci-go-lint`, `make ci-go-build`, `git diff --check`, and `bash docs/design/ci/scripts/check_design_doc_governance.sh`; real live E2E remains externally gated because `make live-e2e-status` reports `state=missing`, `make ci-live-e2e-latest-evidence` finds no full manifest, and `make live-e2e-readiness` fails without `E2E_KUBECONFIG_B64` or `k8s-admin.yaml` (static master-flow and no-mock/no-skip gates pass)
218. ~~**P1** — Make approved batch dispatch and explicit retry crash-safe~~ ✅ **Done** (2026-07-19)
   - Initial approval atomically commits the parent/event claim, normalized execution snapshot, projection refresh, and a parent-scoped River dispatcher.
   - Generic retry atomically commits the active-dispatch guard, failed child ticket/event resets, approved parent/event reopen, projection refresh, and dispatcher; power retry retains atomic per-child job insertion.
   - The dispatcher uses a dedicated queue so old rolling-deployment replicas cannot reserve an unknown job kind, and `REJECTED` children are excluded at handler and SQL boundaries.
   - Parent aggregation takes a parent-row lock before re-reading children, preventing stale worker sync from overwriting a concurrent retry; dispatcher/finalizer guards reject terminal-parent/pending-child and terminal-outcome mismatches before mutating children.
   - Batch cancel validates the durable parent identity/state and commits child cancellation plus parent Ticket/Event/projection synchronization in one transaction.

### CI Checks

- [x] `golangci-lint` passes (`make ci-go-lint`) — verified 2026-06-19
- [x] Unit test coverage ≥ 60% for handwritten Go code — `make ci-go-test` now enforces 62.8% included statement coverage via `check_go_coverage_threshold.sh`; the complete raw `coverage.out` artifact still totals 21.0% because generated/template code stays in the profile
- [x] No data races in required Go test target (`make ci-go-test`) — verified 2026-06-19
- [x] Backend required lane passes (`make ci-backend`) — verified 2026-06-19
- [x] OpenAPI lint, breaking-change check, generated Go/TS types, compat artifact, and contract tests pass (`make ci-api-sync-local`) — verified 2026-06-19 for the current change set
- [x] If 3.1-only features are used, OpenAPI 3.0 compat generation passes (`REQUIRE_OPENAPI_COMPAT=1 bash docs/design/ci/scripts/api-check.sh`) — verified 2026-06-18
- [x] Design docs governance checks pass (frontend path, master-flow links, checklist authority references) — verified 2026-06-18
- [x] Master-flow traceability manifest check passes (`check_master_flow_traceability.go`) — verified 2026-06-18
- [x] Master-flow required-stage test matrix gate passes (`check_master_flow_test_matrix.go`) — verified 2026-06-18
- [x] Governance/static CI suite passes (`make ci-governance`) — verified 2026-06-19
- [x] Embedded KubeVirt schema is current with latest GA patch (`make kubevirt-schema-check`) — verified 2026-06-18
- [x] Frontend lint, typecheck, unit tests, and production build pass (`make ci-frontend-unit-local`) — verified 2026-06-19
- [x] Frontend dependency/dead-code hygiene passes with zero npm audit vulnerabilities (`make ci-frontend-deadcode`) — verified 2026-06-19
- [x] Frontend mock E2E smoke passes (`make ci-e2e-smoke`, 40 Playwright tests) — verified 2026-06-19
- [x] Sequential local PR bundle passes (`make pr-sequential`) — verified 2026-06-19; real live E2E remains externally gated

### Architecture Constraints

- [x] Context correctly passed in in-process async operations ([ADR-0031](../adr/ADR-0031-concurrency-and-worker-pool-standard.md) Rule 2): worker-pool propagation tests + `shepherd-arch/nakedgoroutine`/`semaphoreusage` via `make ci-go-lint` — verified 2026-06-18
- [x] Provider-owned K8s client calls pass bounded operation-timeout contexts (`shepherd-arch/k8stimeout`); adapter wrappers forward caller contexts
- [x] Service layer has no transaction control code (`shepherd-arch/txboundary` via `make ci-go-lint`) — verified 2026-06-18
- [x] Batch operations use ADR-0015 parent-child model with two-layer rate limiting (`check_stage5e_batch_baseline.go`) — verified 2026-06-18
- [x] Frontend batch UI exposes parent/child status, retry failed children, and terminate pending children

### Code-Level Architecture (Code Review Enforcement)

> **Note**: These constraints are enforced during code review, not CI.

| Constraint | ADR | Verification Method |
|------------|-----|---------------------|
| `Bootstrap()` stays orchestration-only and concise | ADR-0022 + pending ADR-0043 note | Manual review |
| Provider interfaces use embedding | ADR-0024 | Verify `KubeVirtProvider` embeds capability interfaces |
| Optional fields use `omitzero` | ADR-0028 | CI: `check_openapi_critical_contract.go` verifies oapi-codegen options and generated Go tags |
| Service layer uses narrow interfaces | ADR-0024 | No dependency on full `KubeVirtProvider` when subset suffices |

### Documentation Sync

- [x] `DEPENDENCIES.md` is the source for dependency/toolchain versions in current-state design docs (`check_design_doc_governance.sh`) — verified 2026-06-18
- [x] Current-state design docs do not hardcode dependency/toolchain versions (`check_design_doc_governance.sh`) — verified 2026-06-18
- [x] `docs/design/frontend/` is used for frontend specs, with no legacy `docs/design/FRONTEND.md` links (`check_design_doc_governance.sh`) — verified 2026-06-18
- [x] `master-flow.md` and phase/frontend docs share consistent status models and endpoint names (`check_design_doc_governance.sh` + traceability/test-matrix gates) — verified 2026-06-18

---

## Prohibited Patterns

| Pattern | Reason | CI Check Script |
|---------|--------|-----------------|
| GORM import | Use Ent only | `shepherd-arch/forbiddenimports` |
| Redis import | PostgreSQL only in V1 | `check_no_redis_import.sh` |
| SQLite usage in tests | PostgreSQL-only policy across runtime and tests | `check_no_sqlite_in_tests.go` |
| Naked goroutines | Use worker pool (ADR-0031) | `shepherd-arch/nakedgoroutine` |
| Wire import | Manual DI only | `check_manual_di.sh` |
| Outbox pattern | Use River directly | `shepherd-arch/forbiddenimports` |
| sqlc outside whitelist | Limited to specific dirs | `check_sqlc_usage.sh` |
| Handler manages transactions | UseCase layer only | `shepherd-arch/txboundary` |
| K8s calls in transactions | Two-phase pattern only | `check_k8s_in_transaction.go` |
| Unsafe semaphore usage | Always `defer Release()` (ADR-0031) | `shepherd-arch/semaphoreusage` |
| Runtime code change without corresponding test delta | Enforce test-first delivery | `check_changed_code_has_tests.sh` |

---

## Core ADR Constraints (Single Reference Point)

> **Purpose**: This section is the **authoritative reference** for critical ADR constraints.
> Other documents (phases, master-flow, notes) SHOULD link here instead of repeating these rules.
> This prevents "content drift" during ADR updates.

| ADR | Constraint | Scope | Enforcement |
|-----|------------|-------|-------------|
| [ADR-0003](../adr/ADR-0003-database-orm.md) | Ent ORM only, no GORM | All data access | CI: `shepherd-arch/forbiddenimports` |
| [ADR-0008](../adr/ADR-0008-postgresql-stability.md) | PostgreSQL-only baseline; no SQLite fallback in test path | Runtime + tests | CI: `check_no_sqlite_in_tests.go` |
| [ADR-0006](../adr/ADR-0006-unified-async-model.md) | All K8s operations via River Queue | External API calls¹ | CI: `shepherd-arch/riverbypass` |
| [ADR-0009](../adr/ADR-0009-domain-event-pattern.md) | Payload is immutable (append-only) | DomainEvent table | Code Review |
| [ADR-0012](../adr/ADR-0012-hybrid-transaction.md) | K8s calls outside DB transactions | UseCase layer | CI: `check_k8s_in_transaction.go` |
| [ADR-0013](../adr/ADR-0013-manual-di.md) | Manual DI, no Wire/fx | All DI | CI: `check_manual_di.sh` |
| [ADR-0015](../adr/ADR-0015-governance-model-v2.md) | Entity decoupling (VM→Service only) + VNC credential transport hardening (no URI query token) | Schema design + Stage 6 VNC flow | CI: `check_stage6_vnc_baseline.go` + Code Review |
| [ADR-0016](../adr/ADR-0016-go-module-vanity-import.md) | Vanity import: `kv-shepherd.io/shepherd` | All Go imports | Code Review |
| [ADR-0017](../adr/ADR-0017-vm-request-flow-clarification.md) | User does NOT provide ClusterID; Namespace immutable after submission | VM Request Flow | Code Review |
| [ADR-0018](../adr/ADR-0018-instance-size-abstraction.md) | InstanceSize hybrid model (indexed columns + JSONB); snapshot at approval | Schema design | Code Review |
| [ADR-0019](../adr/ADR-0019-governance-security-baseline-controls.md) | RFC 1035 naming, least privilege RBAC, audit log redaction | All platform-managed names | Code Review |
| [ADR-0021](../adr/ADR-0021-api-contract-first.md) | OpenAPI spec is single source of truth | All HTTP APIs | CI: `make api-check` |
| [ADR-0025](../adr/ADR-0025-secret-bootstrap.md) | Auto-generate secrets on first boot; priority: env vars > DB-generated | Bootstrap flow | Code Review |
| [ADR-0028](../adr/ADR-0028-oapi-codegen-optional-field-strategy.md) | oapi-codegen with `omitzero`; Go 1.25+ required | API code generation | CI: `make api-check` + `check_openapi_critical_contract.go` |
| [ADR-0029](../adr/ADR-0029-openapi-toolchain-governance.md) | Vacuum for linting, libopenapi-validator | API toolchain | CI: `make api-lint` |
| [ADR-0031](../adr/ADR-0031-concurrency-and-worker-pool-standard.md) | No naked `go` statements; worker pool with context propagation; semaphore Acquire/Release leak-safe | In-process concurrency | CI: `shepherd-arch/nakedgoroutine`, `shepherd-arch/semaphoreusage` |
| [ADR-0034](../adr/ADR-0034-master-flow-spec-driven-test-first.md) | Required master-flow stages must have executable tests or explicit deferred debt; runtime code diffs must carry test diffs | Cross-layer interaction delivery | CI: `check_master_flow_test_matrix.go`, `check_changed_code_has_tests.sh` |
| [ADR-0054](../adr/ADR-0054-minimal-prometheus-observability-baseline.md) | `/metrics` is an operational endpoint outside `/api/v1`; HTTP metric labels must stay low-cardinality | Runtime observability | Unit tests + code review |
| [ADR-0054](../adr/ADR-0054-minimal-prometheus-observability-baseline.md) | PostgreSQL bloat metrics use fixed low-cardinality table labels and scrape `pg_stat_user_tables` without DB schema changes | Runtime observability | Unit tests + code review |
| [ADR-0054](../adr/ADR-0054-minimal-prometheus-observability-baseline.md) | OpenAPI validation failure metrics use fixed phase/code/method/route labels and never raw paths or payload data | Runtime contract observability | Unit tests + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Prometheus alert rules must stay on accepted metrics, fixed labels, and repository governance validation | Runtime observability | CI governance + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Starter Grafana dashboards must stay on accepted metrics, fixed variables, and repository governance validation | Runtime observability | CI governance + code review |
| [ADR-0057](../adr/ADR-0057-opentelemetry-and-correlation-logging-baseline.md) | OpenTelemetry tracing provides HTTP ingress propagation; current observability design extends it with business, DB, River, KubeVirt/provider spans and Tempo-backed storage | Runtime observability | Unit tests + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Prometheus recording rules must stay on accepted low-cardinality metrics and centralize shared HTTP/OpenAPI SLIs | Runtime observability | CI governance + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Prometheus rule tests must cover baseline recording and alert rules with deterministic sample series | Runtime observability | CI governance + code review |
| [ADR-0056](../adr/ADR-0056-observability-deployment-packaging-baseline.md) | Prometheus Operator packaging must stay optional, selector-explicit, and aligned with accepted monitoring rules | Deployment observability | CI governance + code review |
| [ADR-0056](../adr/ADR-0056-observability-deployment-packaging-baseline.md) | Docker Compose monitoring packaging must stay optional, image-version explicit, and aligned with accepted monitoring assets | Deployment observability | CI governance + code review |
| [ADR-0056](../adr/ADR-0056-observability-deployment-packaging-baseline.md) | Prometheus config validation must prove scrape config syntax and rule-file loading without changing deployment container paths | Deployment observability | CI governance + code review |
| [ADR-0056](../adr/ADR-0056-observability-deployment-packaging-baseline.md) | Prometheus Operator rule parity validation must prove `PrometheusRule.spec.groups` matches native recording and alert rule files | Deployment observability | CI governance + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Prometheus alert runbook links must point to existing local Markdown anchors | Deployment observability | CI governance + code review |
| [ADR-0055](../adr/ADR-0055-prometheus-rules-and-grafana-dashboard-baseline.md) | Grafana starter dashboard panel queries must parse as PromQL through `promtool check rules` wrappers | Deployment observability | CI governance + code review |
| [ADR-0054](../adr/ADR-0054-minimal-prometheus-observability-baseline.md) | River queue metrics must expose only aggregate `queue`, `state`, and `kind` labels and must not expose job args, IDs, tickets, VMs, namespaces, clusters, or payload data | Runtime observability | Unit tests + CI governance + code review |
| [ADR-0057](../adr/ADR-0057-opentelemetry-and-correlation-logging-baseline.md) | HTTP request completion logs must use bounded correlation fields and normalized routes; raw paths, payloads, users, tickets, VMs, namespaces, and clusters stay out | Runtime observability | Unit tests + CI governance + code review |
| [ADR-0057](../adr/ADR-0057-opentelemetry-and-correlation-logging-baseline.md) | River worker completion logs must use bounded lifecycle fields and Shepherd-owned trace metadata; raw args, metadata, payloads, error strings, tickets, VMs, namespaces, and clusters stay out | Runtime observability | Unit tests + CI governance + code review |

> ¹ **ADR-0006 Scope Clarification**: "All writes via River Queue" applies to operations requiring external system calls (K8s API).
> Pure PostgreSQL writes (e.g., Notification, AuditLog, DomainEvent insert) are **synchronous** for transactional atomicity.
> See [Phase 4 §6.3](phases/04-governance.md#63-notification-system-adr-0015-20) for detailed rationale.

---

## Explicitly Not Doing

The following items are moved to [RFC directory](../rfc/):

| Item | Status | Notes |
|------|--------|-------|
| Complex Admission Rules | 📋 RFC | Phase 2 only basic validation |
| Config Hot-Reload (Basic) | ✅ Done | Log level, rate limit params support hot-reload |
| Config Admin API | 📋 RFC | API dynamic modification, see [RFC-0006](../rfc/RFC-0006-hot-reload.md) |
| Notification/Approval Plugin System | 📋 RFC | Implement as Service first |
| Frontend Refactor | 📋 RFC | Consider after backend stable |

---

## Progress Tracking

| Phase | Status | Completion Date | Verified By |
|-------|--------|-----------------|-------------|
| Phase 0 | ✅ Complete | 2026-02-09 | CI green (go vet/build/test) |
| Phase 1 | 🔄 Partial (~99%) | - | 33 Ent schemas + Ent schema standards/codegen/dynamic-query safety gates + Go/TS API types + ADR-0028 generated-type/pointer-audit gates + frontend testing toolchain + cluster credential runtime + provider plugin config boundary done; remaining work is deferred V2 schemas/deployment items |
| Phase 2 | 🔄 Partial (~73%) | - | Basic VM CRUD + SSAApplier + VMRenderer + AuthProvider Admin + KubeVirt instance type/preference catalog reads + label-based pending adoption discovery/periodic scan + admin adoption management + bounded provider K8s operation timeouts + provider unit test lane + V1 River queue concurrency + i18n/Atlas baselines done; Snapshot/Clone/Migration deferred; ResourceWatcher docs superseded in V1 by ADR-0038 polling baseline |
| Phase 3 | 🔄 Partial (~89%) | - | Core DI/UseCase + ADR-0012 atomic approval + sqlc + InstanceSize handler + analyzer-backed DI/sqlc CI gates done; River worker concurrency + V1 degradation baseline done; DB-backed VM API CRUD handler success paths + VMService create approval/worker e2e done; per-cluster semaphore/degradation UX deferred |
| Phase 4 | 🔄 Partial (~98%) | - | Approval/Audit/Delete/Notification/Batch/VNC hardening/Catalog Scope/Cluster Policy/VM Status Sync (ADR-0038)/Template+InstanceSize validators/User My Requests API done; full resource reconciler, rich revision diff/compression service, and template lifecycle states deferred |
| Phase 5 | ✅ Complete for current scope | 2026-05-08 | Backend auth ✅, API gen 140 endpoints (ADR-0028 omitzero) ✅, 32 App Router page files (incl. AuthProvider/RateLimit/Batch/Profile/VNC/Admin Adoption routes), login/protected API and admin CRUD mock E2E verified |

---

## Quick Links

- [DEPENDENCIES.md](./DEPENDENCIES.md) - Version pinning (single source of truth)
- [ci/README.md](./ci/README.md) - CI scripts documentation
- [examples/](./examples/) - Reference implementations
